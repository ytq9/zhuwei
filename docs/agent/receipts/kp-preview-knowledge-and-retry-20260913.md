# 本地 KP 认知表达与归档重试排查

2026-09-13；工作树 `local-preview-changes-8206cb`，分支 `claude/local-preview-changes-8206cb`，基线 `5aaa62d`。用户明确允许直接修复运行 3000 的预览工作树；根目录 `cloudflare` 工作区的已有改动未参与本次修改。

## 结论与边界

- 用户指定 DB7453。取物与观察已结算，失败发生在后续旁白生成/恢复阶段。原日志两次记录 `modelTransient`，分别耗时 17,046 ms、15,419 ms；未保留足够的 Provider 原因，不能据公开的 `NARRATION_PROVIDER_TIMEOUT` 断定必然是超时或具体服务故障。
- 保存的观察结果已包含没有感官依据的秘密解释，confidence 也混入了角色未知概念；这是上游知识创作越界，旁白又转述了这些材料。仅改正文写法不能纠正已经冻结的结果。
- 归档在旧旁白 stage 与当前 `VNEXT_KP_WORKFLOW_HASH` 比较处失败。原始日志在本次修改前就有归档失败；本次 Prompt 变更也改变了完整工作流哈希。失败不得通过跳过校验或冒用旧哈希消除。
- 归档恢复入口忽略已保存的重试时间，校验失败又进入完整构建重复校验。两个房间反复执行，workerd 持续接近满 CPU；本地页面曾在 10–30 秒内不返回响应头。只重启未解决。

## 已验证的叙述候选与已启用修复

依据 [SPEC 0001 §9](../../specs/0001-llm-kp-responsibility-contract.md)、[SPEC 0005 §6.3](../../specs/0005-world-facts-and-knowledge.md) 和 [SPEC 0016 §8.3](../../specs/0016-part-c-compound-actions-and-claims.md)：

- 提案 guidance v28 和 schema 字段说明要求先描述角色所得感知；观察不必附加推断。解释及不确定性均不能借用 KP 私有背景，否定句也不能暗示未获授权的秘密。
- 旁白 Prompt policy v14 的生成与独立复核共用认知表达约束：自然保留证据限制，不播报高/中/低置信等级、不反复声明“只是推断”；明确且有依据的局部排除结论仍可直接说明。
- 未改变 Claims、事件或 schema 的结构，未在恢复时过滤或重写冻结内容。

叙述候选改变完整工作流哈希。为避免在用户决定旧房处理方式之前让 DB7453 失去访问能力，最终将上述三个 Prompt 文件恢复到基线，完整候选保存在 `.wrangler/kp-preview-20260913/knowledge-expression-v28.patch`，尚未启用。已读取 DB7453 目录绑定并确认恢复后的源码 manifest 与其逐字匹配；没有修改数据库绑定。

依据 [SPEC 0011 §§1、2、6](../../specs/0011-reliability-correction-observability-and-evaluation.md)：

- 已失败的 host binding 当场拒绝，避免完整构建再校验一次。
- 完整性错误保留待归档状态并退避 60 秒；普通临时依赖错误仍退避 1 秒。恢复入口遵守保存的下次重试时间；显式导出所需的同步 flush 仍可尝试执行。
- 未放松归档完整性检查，未把失败标成完成。已删除全部临时诊断日志。

## 验证

1. `npx tsx --test tests/kp-vnext-prompt-contract.test.mjs tests/kp-vnext-narration.test.mjs tests/narration-provider-failure.test.mjs`：55/55，通过冻结结果首次失败、成功发布、恢复只重新发布等已有路径。
2. 新增归档退避用例先失败（实际仅约 993 ms），修复后通过；最终 integrity/transient 两例验证恢复不抢跑、驱逐后时间保留、使用本地真实 D1 最终写入归档。现有超大 ledger、相同来源不重复归档两例定向通过；同源码下 generation 并发例也通过。
3. 曾运行整个 `archive-do-resume-v2.test.ts`，新用例最初使用的旧 fake D1 不支持现行归档 SQL，调整为真实本地 D1 后通过。文件中另五项失败与现存 `/tmp/zhuwei-wire-baseline-vitest.json`、`/tmp/zhuwei-wire-verified-vitest.json` 逐项一致；它们依赖旧 fake D1，本次未扩展改写这些夹具，也不声称整个归档文件通过。此前基线来源见[分组步骤回执](./vnext-grouped-steps-wire-validation.md)。
4. 重启后的 DB7453 未登录 HTTP 页面返回 200，响应头约 26 ms；这是登录入口可达证据，不能当作认证后房间可玩或旁白已送达。后台同房归档单次失败约 6.4–6.7 秒，后续两次间隔约 68 秒，与 60 秒退避及执行耗时一致。未证明原 Provider 失败由归档卡顿直接造成。
5. `npm run spec:check`（0 错误、7 既有警告）、`git diff --check` 与文档链接基线检查通过；链接检查仍报告另一份既有提案中的 1 条基线断链。没有公共类型结构变化，未跑 typecheck；未跑全项目测试、Lint、production build、部署、push 或远端 migration。Prompt 的 55 项测试及真实探针验证的是候选补丁；归档测试中的绑定由当时源码生成，最终保留的归档实现没有进一步改变。

## 有界真实模型证据

实际调用 4 次，合计输入 14,594、输出 1,953 tokens；没有执行、发布或重试真实游戏行动：

| 调用 | 结果 |
| --- | --- |
| 原冻结请求生成 | HTTP 200，6,175 ms；本次未出现置信等级，但仍转述冻结材料中的越界解释，未发布 |
| 旧复核规则 + 置信等级措辞 | 错误放行，验收断言失败 |
| 新复核规则 + 同一坏措辞 | 正确返回 `PRESENTATION`，定位具体原句 |
| 新复核规则 + 保留证据限制的自然表达 | 放行 |

本地计划上限 4 次/5 分钟，实际使用 4 次；从首次请求开始到最后完成 312,140 ms，超出声明时长约 12 秒，不能记作严格满足时间预算。没有继续采样。上述结果只证明这些样例，不构成稳定成功率或新上游提案的全面验证。

## 尚待处理

DB7453 仍绑定旧工作流，旧记录中的越界推断和未送达回复没有被修改。新版 Prompt 的完整 manifest 与旧房不匹配，因此暂存候选而没有保留在运行源码中；不能声称新规则已经在该房生效。用户已收到“保留进度，准备有审计的升级与更正”或“退役本地试玩数据，重新开局”的选择，尚无本次数据处置决定。

执行前仍须遵守 SPEC 0011 §4 的明确迁移事件以及 §7 的更正审计，不直接更新旧工作流字符串、不静默改知识、不把重新裁决包装成旁白重试。未执行旧房删除、迁移、更正，也未点击其重试按钮。

私有原始请求、响应、脱敏测量与测试日志保存在忽略目录 `.wrangler/kp-preview-20260913/`；不提交凭证、私人叙述或冻结世界材料。预览用原有捕获配置重启，恢复匹配工作流后的当前日志 `dev-preserved.log`。
