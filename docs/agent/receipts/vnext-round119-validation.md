# round119：隐蔽动作检定失败，莉安记下了她看见的事

2026-09-24。用例 `daily-hidden-act`，调用上限 10，真实 HTTP 路径，`deepseek-v4-flash`。未部署、未 push、未做远端 migration、未改 Secrets。承接 [round116–118](./vnext-round116-validation.md)。

## 先查了修订请求为什么超预算

用 round118 保存的冻结上下文和三次回复在本地重建被拒的修订请求，按 Room 同一估算口径测得约 58,500（上限 58,000）。它等于填写请求（五种表单，55,840）再加草稿、诊断工单和补丁工具。SPEC 0016 §7.2 要求修订携带与填写相同的冻结上下文和所选表单，所以只能在各阶段共用的地方减。

模型视图和表单里有重复或模型用不到的内容，`fd85758` 和 `7eced83` 把它们去掉，Room 读取的冻结上下文不变：

| 内容 | 处理 | round118 约减 |
| --- | --- | --- |
| 服务器 hash（32 处） | 任何层级都不发给模型 | 770 |
| 模组地点锚点的几何 | 与场景条目完全相同时写 `sameAsEntryRef` | 780 |
| 已有独立条目的 NPC 定义、决策视图里的身份 | 只列 ref | 900 |
| 决策视图里的知识目录 | 只列 ref（与持有者目录条目一致） | 400 |
| `references.domains`（服务器的引用类型索引） | 不发给模型 | 1,270 |
| 物品条目引用重复九次的说明、可空引用重复的 `{kind:'none'}` 说明 | 删去重复，规则在共享指导里说一次 | 1,400 |

同一份 round118 数据重建后：选择 38,049 → 33,609，填写 55,840 → 49,966，修订约 58,500 → 52,715。按能力的体积棘轮（系统提示加表单，不含上下文）全部下降，social −910，全量 −1,837。

表单还把“拥有共享检定的步骤”改成“同时写两个结果的那个 observe/social/worldInteraction 步骤”：round118 的首稿把交谈步骤的失败写成 none，推测是把巧手检定当成取物步骤“拥有”的。

## round119（源码 `fd85758`，即 vnext-9，尚未去掉 `domains`）

6 次调用，181,163 输入 / 3,105 输出 token，通过。

| 调用 | 用途 | 估算 | 实际输入 | 输出 |
| --- | --- | --- | --- | --- |
| 1 | 选择：inventoryOperation、social、observe，加载奈斯视图 | 34,884 | 29,534 | 104 |
| 2 | 填写轮补选：加载瓦罗视图和莉安一条记忆 | 51,643 | 46,072 | 106 |
| 3 | 填写 | 54,477 | 48,752 | 1,098 |
| 4 | 修订（整稿替换） | 57,551 | 51,213 | 1,625 |
| 5–6 | 旁白生成与审核（pass） | 3,449 + 3,172 | 2,794 + 2,798 | 120 + 52 |

- 首稿仍把交谈步骤的失败写成 none（表单措辞改后第一个样本）；修订一次改对。
- 裁决：敏捷检定 DC 15。d20 掷出 4，失败。
- 莉安得到自己的感官证据：“莉安看见玩家借着替她父亲整理衣领，把手伸到遗体的嘴边去碰那片黑橡叶。”
- 她听到的只有那句问话（`actorSpeech`）：“你父亲生前常来守灵厅吗？”；“装作”“趁莉安不注意”不在她的记忆里。用例的听者断言通过。
- 莉安的回应：“你手别碰他。那片叶子是我们家的事，不用外人替他整领子。”
- 失败分支的 observe 步骤写的是玩家看见瓦罗抬眼看向自己的手，不是瓦罗自己的见证记录，所以瓦罗的记忆里没有这件事。
- 旁白：“你刚朝赫斯那边伸手，想装作替他理领子，莉安立刻拦住你：……这话一出，你顺叶子进袖子的打算没能实现。你随后问：……莉安没有对这个问题作任何回答。”后接瓦罗抬眼的描写。审核通过。

## 没有覆盖的

- `7eced83`（去掉 `references.domains`）只有本地重建测量，没有真实批次。用它重建 round119 的修订请求为 56,277 / 58,000：加载了两名旁观者视图时余量仍只有约 1,700。
- 首稿漏写检定失败分支两次都发生（round118 旧措辞、round119 新措辞），每次多花一次约 5 万 token 的修订调用。
- 上下文仍按词面把不在场的“神龛回声”及其所在的神龛场景带进来（回声的描述里有“莉安”），约 1,600。
- 旁观者见证写成了玩家的感知；旁白“作任何回答”“打算没能实现”偏生硬。

私有证据在 `/var/folders/lc/…/zhuwei-story-room-probe-mBlYqN`，重启即失。
