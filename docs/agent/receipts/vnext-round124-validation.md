# round124：新的上下文写法在真实调用里可用

2026-09-25。用例 `daily-spoken-intent`，行动文本与 [round123](./vnext-round123-validation.md) 相同：“我向lian介绍，我是一名剑湾法庭的猎魔人，今天特意来调查她父亲死亡的事件，名字叫散木，希望她可以尽力配合”。调用上限 10，真实 HTTP 路径，`deepseek-v4-flash`，源码 `6c8ce9e`（[ADR 0048](../../adr/0048-each-character-carries-its-latest-six-rounds.md)）。未部署、未 push、未做远端 migration、未改 Secrets。工作区里另一个会话未提交的改动只涉及非 DeepSeek 模型的传输方言，不经过本用例。

## 这一批验证什么

`6c8ce9e` 改了发给模型的上下文写法（vnext-12）和选择阶段指引（v48），并去掉了整份原话集合。这一批只确认模型能按新写法选择、填表、修订，不出新的错误。它是新开的房间，没有对话历史，所以“最近六轮”和去重在这里都看不出效果。

## 结果

5 次调用，92,060 输入 / 3,048 输出 token，通过。

| 调用 | 用途 | 输入 | 缓存命中 | 输出 |
| --- | --- | --- | --- | --- |
| 1 | 选择：social，补选莉安的视图 | 21,843 | 256 | 75 |
| 2 | 填写 | 30,527 | 14,720 | 2,131 |
| 3 | 修订一轮（补丁） | 32,971 | 29,568 | 588 |
| 4–5 | 旁白生成与审核（pass） | 3,315 + 3,404 | 768 + 640 | 202 + 52 |

- 填写阶段的上下文是 vnext-12：continuity 只有裁决先例、有意义的失败、未解决威胁三项；莉安的摘要目录每行只有摘要和编号（m1–m3）；已读记忆只列条目引用；记忆条目不再带持有者、knowledgeRef 和事件号。
- 裁决：说服检定，魅力，DC 7，`checkStep: "social"`。成功，莉安记下散木的姓名和来意，交出铜钥。旁白审核通过。
- 修订一轮：首稿又把同一段交谈在 `steps.social` 里写了一遍，与 round123 首稿的第一处错相同，和本次改动无关。

选择与填写两次调用的输入和 round123（21,822、30,253）基本相同，符合“没有历史时写法变化很小”的预期。

## 没有覆盖的

- 超过六轮的长对话：旧记忆只剩摘要时，KP 会不会按编号调取。要在有历史的房间里看，本地预览那一桌可以接着测。
- 未点名时默认加载最近交谈对象：新房间没有交谈历史，这一批没有触发。

私有证据在 `/var/folders/lc/…/zhuwei-story-room-probe-dLvO84`，重启即失。
