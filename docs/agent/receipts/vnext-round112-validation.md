# round112：隐蔽动作过检定并得手；失败分支仍是空壳

2026-09-23。源码 `7790ef3`，工作区无未提交改动。承接 [round109–111](./vnext-round109-validation.md)，用例 `daily-hidden-act`，调用上限 10。未部署、未 push、未做远端 migration、未改 Secrets。

## 结果：通过

7 次调用，137,725 输入 / 1,476 输出 token。

- 裁决：敏捷（巧手）DC 14 检定。取叶的库存操作绑定 `onSuccess`，与 v37 指导一致。
- 玩家掷出 17，成功。叶子到手，莉安没有察觉，没有见证记录；她只记下问话“你父亲生前常来守灵厅吗？”，没有“装作”“趁莉安不注意”。
- 旁白首稿写了“趁莉安没注意”，审核判为冻结材料之外的断言；修稿后通过。

## 发现：失败分支没有真正写出来

交谈步骤承担这次检定的两个分支，但失败分支是占位：`outcomeCode: "none"`、`summary: "none"`、莉安沉默，也没有 `onFailure` 绑定的观察或世界交互步骤记录莉安看见了什么。与此同时，裁决自己的 `failureOutcome` 写的是“她看清玩家从遗体齿间取叶，立刻质问并拒绝再谈父亲的事……关系明显变坏”。

本批掷骰成功，没有走到失败分支。若失败，结果会是：叶子没拿到（`onSuccess` 未生效）、莉安沉默、没有任何记录，与冻结的失败说明不一致，旁白很可能再次被审核拒绝、行动作废（同 round111）。

校验目前接受以字符串 `"none"` 填写的分支字段，所以这种空壳分支能通过。

## 收尾

私有证据在 `/var/folders/lc/…/zhuwei-story-room-probe-dmlvON`，重启即失。
