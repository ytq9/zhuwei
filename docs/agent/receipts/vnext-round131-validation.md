# round131：DC 按 5e 区间取值，档内按情境定数

2026-09-26。三句行动，每句一个新房间，用例 `daily-spoken-intent` 换行动文本，`tools/run-story-room-probe.mjs --live --max-calls 6`，真实 HTTP 路径，`deepseek-v4-flash`，源码 `aa17d5d`（指引 v53）。在 `cloudflare` 的干净克隆上运行，不含主工作区里另一个会话未提交的改动。未部署、未 push、未做远端 migration、未改 Secrets。

## 这一批验证什么

[round127–130](./vnext-round127-validation.md) 里每档只给一个数（很容易 5、容易 10、中等 15……），模型定的 4 个 DC 全是 15。用户要求只给区间。`aa17d5d` 改为：DC 按难度落在 5e 区间，很容易 1-5、容易 6-10、中等 11-15、困难 16-20、很难 21-25、几乎不可能 26-30，具体数值只看行动本身和当前情境。区间按 5e 参考表对齐（5 是很容易）。

## 结果

| 句 | 行动 | 调用 | 输入（缓存命中）/ 输出 token | 裁决 |
| --- | --- | --- | --- | --- |
| 1 | 骗莉安说是她父亲的老朋友，想让她说出父亲最后的话 | 4 | 62,476（21,120）/ 1,550 | 魅力 **DC 16**，未标技能；骰 9+1 失败，莉安不接受这层关系、对你提防 |
| 2 | 「我一拍桌子，逼莉安马上说出后院酒窖里藏着什么，不说今晚就别想安生。」 | 4 | 62,715（21,248）/ 1,471 | 威吓 **DC 15**；骰 1 失败，莉安收起铜钥、不再接话，记住你逼过她 |
| 3 | 「奈斯提到遗嘱时，我盯着他的手和眼神，想看出他是不是在撒谎。」 | 4 | 58,126（20,992）/ 2,191 | 洞察 **DC 13**；骰 13+0 成功 |

合计 12 次调用，183,317 输入（缓存命中 63,360）/ 5,212 输出 token，3 句全部通过，都没有修订。

- 三个 DC 分别是 16、15、13，落在困难和中等区间，不再都是同一个数。
- 同一句欺瞒，v49 定 14，v50 定 15，v53 定 16。

## 没有覆盖的

- 每句一个样本；没有取样很容易、很难和几乎不可能的行动。
- 第 1 句模型只用魅力、没有标欺瞒技能，技能选择本次没有另查。
- 修复没有部署，线上仍是 v48。

私有证据在 `/var/folders/lc/…/zhuwei-story-room-probe-{IGqbLk,r7QupF,4hQ4C0}`，重启即失。
