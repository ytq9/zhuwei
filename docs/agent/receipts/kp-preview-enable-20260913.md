# 本地 KP 认知表达修正启用与新房验证

2026-09-13，承接[排查回执](./kp-preview-knowledge-and-retry-20260913.md)。用户明确答复“不用保留进度”，允许本地试玩重新开始。继续在已授权的 `claude/local-preview-changes-8206cb` 工作树处理，未触及根 `cloudflare` 工作区、远端资源或其他账号。

## 启用结果

- 浏览器在本轮操作前已显示 DB7453、DU5GZ7 删除通知，用户自己的桌子列表为空；本地 D1 核验 DB7453 目录记录为 0。删除不是本轮代理执行的操作，没有额外删库或修改旧房历史。
- 原样应用此前验证过的 `knowledge-expression-v28.patch`。提案 guidance v28、旁白 policy v14 与 schema 描述现已启用；归档失败退避修复同时保留。
- 通过用户已登录的正常 Chrome 页面创建 PRX4HR，锁定默认人类战士人物卡“散木”，正常开局。新房目录的完整 KP manifest 与当前源码逐字一致。没有增加账号、伪造会话、改绑定绕过校验或迁移旧事实。
- 既有 55 项 Node 用例及复核模型对照对应这份原样应用的候选；新批起止四份相关源码 SHA-256 完全相同。此前归档定向证据继续成立，不重复计算为新增通过。

## 真实批次

先保存计划：最多 2 条行动、12 次物理调用、600,000 输入/50,000 输出 tokens、¥5、10 分钟；本地每 HTTP 临时限制 6 次，首个失败停批，仅允许一次能区分原因的对照。原有私有捕获服务保持不变。

实际只提交“拿起黑橡叶仔细查看。”，4 次调用，118,100 输入、2,842 输出 tokens：

1. 首次选择 observe，随后补选 completeObject。
2. 填写稿 JSON 在偏移 2706 出现括号结构错误；服务器返回准确的 `JSON_SYNTAX` 位置。
3. 修订稿改为额外的 `arguments` 字符串包裹；其内部仍是同一份无效 JSON。声明的工具参数只有 decision/steps，故正确拒绝为 `REPAIR_OUT_OF_SCOPE`，最终公开 `PROPOSAL_REPAIR_EXHAUSTED`。
4. 玩家界面明确显示行动未提交；本地权威 events、receipts、delivery audiences 均为 0。没有扣资源、发布旁白或提交观察知识。原填写稿未出现本次禁止的置信等级或仪式解释，但无效草稿不能当作正式回复通过证据。

这次错误与原 DB7453“已经结算而旁白尚未送达”处于不同阶段。原始请求和响应保留，未用自动补括号、解包伪造合法结果或重新采样隐藏失败。

计划用“从赫斯齿间取出黑橡叶，收进我的背包。”做一次取物链路对照，但浏览器输入控制多次超时，剩余批次时间不足，没有发送。到达时间上限后停止追加调用；最终清空未发送的输入并关闭开发者工具。不会把未执行对照或登录页 HTTP 200 计作端到端成功。

## 当前限制与证据

新房已经开局且使用修正版，仍在初始世界状态；“取叶并查看”的真实完整链路本次没有通过，阻塞点是模型返回的提案 JSON 和无效修订。没有据此扩展更改提案解析/修订合同，也不声称 KP 的全部生成稳定性已经解决。建议后续单独针对已保存的 malformed 提案及其修订做定向改进与一次有界原场景复验。

私有计划、四份捕获、源码校验及状态核验位于 `.wrangler/kp-preview-20260913/restart-validation/`。模型没有提供费用金额，本回执不把费用记为零。测试结束移除临时每 HTTP 调用限制，预览恢复原有捕获配置。`git diff --check`、文档链接基线检查无新增问题；未执行部署、push、远端 migration 或全量回归。
