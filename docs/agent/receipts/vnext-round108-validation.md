# round108：莉安记下了她看到的事

2026-09-23。源码 `613610e`，工作区无未提交改动。承接 [round107](./vnext-round107-validation.md)，同一用例 `daily-witnessed`，调用上限 10。

## 结果：第一步通过，第二步审核前用完调用额度

| 调用 | 用途 | prompt tokens | 输出 |
| --- | --- | --- | --- |
| 1 | 第一步 offer（observe、inventoryOperation、completeObject） | 31,355 | 93 |
| 2 | 第一步填写 | 38,408 | 1,646 |
| 3–4 | 第一步两次补丁修订 | 40,350 + 42,814 | 121 + 122 |
| 5–6 | 第一步旁白生成与审核（pass） | 2,594 + 2,642 | 161 + 52 |
| 7 | 第二步 offer（social） | 31,349 | 83 |
| 8 | 第二步填写轮补选（未增加类型） | 39,081 | 83 |
| 9 | 第二步填写 | 38,348 | 596 |
| 10 | 第二步旁白生成 | 2,447 | 54 |
| **合计** | **10** | **269,388** | **3,011** |

停止码 `PROBE_BUDGET_EXHAUSTED`：第二步的旁白审核是第 11 次调用，超过上限，未发出。第二步旁白已生成但未经审核，不算已发布。

### 第一步：莉安得到了自己的记录

模型在 `observe` 里为莉安写了一条见证：“这个外乡人当着你把叶子从你父亲嘴里取了出来，举到烛光下翻看背面。”行动者意图和只有行动者看到的叶背纹路都不在里面。功能断言（莉安的知识有变化）通过。

模型还为行动者写了一条关于莉安的观察：“莉安一直看着你伸手去碰她父亲的嘴……她的手在膝盖上停了一下，没出声，也没走开。”这是 KP 在提案里定下的 NPC 可见反应，已冻结，旁白照此表达，没有另编。

### 第二步：记忆到了，莉安选择克制

第二步只说“你好，莉安。”。填写请求的冻结上下文里有这条见证事实，模型在莉安台词的 `basis` 里也引用了它。莉安的回答是：“你好。……我记下了。你们是路过，还是专门来给赫斯守灵的？今晚镇公所那边有话要说，你们要是办事的，就先等一等。”动机写的是先弄清来路再决定交不交铜钥。

她没有直接提取叶子的事。这和人物卡的行为约束一致（先问你们是谁；被呵斥就把话咽回去），属于模型对人物的演绎，不是信息缺失。按用户“不规定句式”的要求，不为此加提示规则。

## 没有覆盖的

- 第二步的旁白审核与发布（额度用完）。
- 纯 `inventoryOperation`、没有观察或世界交互的行动（例如只说“我拿起叶子”）仍没有位置写见证。
- 检定失败分支（round105/106 遗留）。

## 收尾

未部署、未 push、未做远端 migration、未改 Secrets。私有证据在 `/var/folders/lc/…/zhuwei-story-room-probe-sCSHMe`，重启即失。
