# round116–118：交谈对象看见了什么，走通了结构，卡在预算

2026-09-23。三批都是用例 `daily-hidden-act`，调用上限 10，真实 HTTP 路径。均未部署、未 push、未做远端 migration、未改 Secrets。承接 [round113–115](./vnext-round113-validation.md)。

## 这一段做了什么

`8c80e08`：交谈结果的每个分支带 `npcPerceives`，写交谈对象在该结果里亲眼看到的行动者举动；lowering 把它变成这名 NPC 自己的感官证据，只对她可见。不再为此多选 observe 表单。`2cdc550`、`a460576` 见下。

## round116（源码 `8c80e08`）

3 次调用，127,768 输入 / 1,886 输出 token。

- 首稿仍没有同时写两个结果的步骤；改进后的诊断让唯一一次修订一步修好：交谈步骤改为 `always`，失败分支写全。
- 成功分支 `npcPerceives`：“她看见你俯身替赫斯理了理衣领，像在照顾客人，没看见你从遗体嘴边拿走任何东西。”
- 失败分支 `npcPerceives`：“她看见你的手从赫斯遗体嘴边收回，指间夹着一片黑橡叶，你把它拢进袖口”；莉安：“你把那叶子拿出来了。那是我父亲嘴里的东西，你要它做什么？”
- 掷骰后 Room 提交报 `projectionIntegrity`：`WorldInteractionResolved` 要求交谈结果的 `sensoryEvidence` 为空，回放已提交范围时拒绝了新产生的感知。本地用 Room 社交测试加失败分支感知复现，`2cdc550` 改为要求它与所选分支的感知完全一致。修复后该 Room 测试通过，且莉安的感知不进入玩家视图和旁白。

## round117（源码 `2cdc550`）

1 次调用，32,785 输入 / 123 输出 token。选类型阶段照“交谈对象以外的人可能察觉……同时选 observe”选了四种表单，并加载奈斯、瓦罗的决策视图，填写请求超预算。`a460576` 删掉这句。

## round118（源码 `a460576`）

3 次调用，132,534 输入 / 1,047 输出 token。模型自己仍选了四种表单；草稿里交谈步骤只有成功分支（`npcPerceives` 写得合理），需要一次修订，而修订请求超出输入预算，行动被拒。

## 现状

- 交谈对象的感知：本地从校验、lowering、Rules、回放、投影到 Room 全部测通（强制失败掷骰）；真实模型在 round116 写出了正确结构。
- 尚未有一次真实批次完整走通“检定失败并被察觉”。当前阻碍不在 NPC 感知本身：
  1. 首稿经常漏写检定所属步骤的失败分支，要靠修订；
  2. 选了四种表单的动作，修订请求会超出输入预算。

私有证据分别在 `/var/folders/lc/…/zhuwei-story-room-probe-uFIBb9`、`…-zmuSec`、`…-YKHIcJ`，重启即失。
