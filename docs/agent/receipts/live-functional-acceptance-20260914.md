# 真实模型功能验收：酒馆短支线样例

日期：2026-09-14。用户本轮明确要求“去用真实模型验收一下”。结果：**本轮完整行动体验验收失败；行动已提交，旁白未发布，动态支线未触发。** 原始失败保留，没有更换措辞重采、修改模型结果、调用诊断对照或改动业务源码。

## 源码、环境与批次边界

- 分支 `cloudflare`，HEAD `ff78223102950adb4605d8fee9b8c837a34d55fa` 加已有工作树差量。测试和运行器沿用本次[测试重组](test-suite-reorganization-20260914.md)后的实际文件。
- 381 个运行源码及探针文件的初末清单完全一致：`sha256:ef4111b34055b14636df4e90515dc295ec7f683b77453a8ba368b6f9823d18be`。源码清单包含未提交的现役探针文件，不包含凭据。
- Model Profile：`authoritative-kp-deepseek-vnext-local-v1`；workflow hash：`sha256:e095c545a29f4ca69210164b9f65db3549b609b0edc3438034cc1aedbc6d4a00`。
- runtime：`runtime-srd51-2014-authoritative-vnext-stage3`，hash `sha256:76c2b58e853c5875dc00b99892b6d109dbf5809fb7b09d6cdc8bda42111e9566`；模组 `black-oak-will / social-resolution-v1`。
- 请求模型名保持 `deepseek-v4-flash`；五次响应均返回 `deepseek-flash`。当日[官方文档](https://api-docs.deepseek.com/zh-cn/quick_start/pricing)说明旧别名现由 DeepSeek-V4.1-Flash 提供服务，因此本次不能称为旧版 V4 Flash 的冻结版本验收。模型切换是否导致本次失败尚未通过对照证明。
- 一个根行动，成功后原提交精确重试一次；上限 10 次模型调用、960,000 输入 / 64,000 输出 tokens、单次 45 秒、整轮 540 秒。按核验的高峰标价及 token 上限，最坏费用估算 ¥2.432。首次最终失败、未知 usage、超时、预算耗尽或源码漂移立即停止。
- 注册和会话使用真实路由；房间与角色由现有夹具通过 Room 初始化和 D1 设置。行动从带 Cookie 的实际游戏 `POST` handler 进入正常 Room、Provider、Rules 和发布链；没有通过浏览器或网络监听端口验证建房、建卡、开局。外部模型通过仅限本机的桥接调用真实 DeepSeek，测试库和 Room 均在本地隔离。

批次计划在调用前写入本地私有目录 `.wrangler/live-acceptance-20260914/plan.json`。原始请求、响应、状态和日志保存在同目录 `live-evidence/`（目录 0700、文件 0600），不进入版本库；公开回执仅记录必要的脱敏结果。未部署、push、修改远端资源或访问生产游戏数据。

## 固定输入与执行结果

样例 `short-local-conflict` 的原始玩家意图：

> 今晚我先留在黑橡居酒屋，花些时间协助莉安理清近期日常经营中尚待解决的问题。我到炉边说明来意，请她挑一件今晚值得着手的小事，把经过和牵涉的人告诉我；若有人说法不同，我先分别核对，弄清各自的难处。等知道具体情况后，我再决定如何处理。

执行命令：

```bash
node --import tsx tools/run-story-room-probe.mjs --preflight
node --env-file=.dev.vars --import tsx tools/run-story-room-probe.mjs --live --case short-local-conflict
```

预检 exit 0，1 项通过、0 模型调用。真实运行于北京时间 16:15:45 开始，Vitest **1 项失败、0 通过、0 跳过**，exit 1；总时长 39.09 秒，用例时长 36.35 秒。调用顺序为两次能力/材料选择、一次提案填写、一次受限修订、一次旁白生成。所有 Provider HTTP 状态均为 200，有完整 usage；没有进入故事初稿、故事审核或旁白审核。

| 功能或事实 | 本次判定与证据 |
| --- | --- |
| 实际行动与保存 | 游戏 HTTP 200，`action=committed`。8 个事件、3 份回执保存；主时间线推进 600,000,000 微秒（10 分钟），另一时间线不变；无随机批次 |
| 耗时与交谈衔接 | 事件依次为 ActivityStarted、FictionTimeAdvanced、ActivityCompleted、两组 SourceClaimCreated / KnowledgeAcquired、WorldInteractionResolved；只证明这一个实际轨迹 |
| NPC 与玩家知识 | 莉安的知识发生变化，双方话语分别形成来源主张和取得记录；未据此判定全部内容真实、NPC 知识边界或跨轮声口通过 |
| 动态支线（KP-C01 等） | **未触发目标**：模型选择普通 social，创作作业 0、故事库条目 0，未发生故事准备或接入；不将其记作支线成功或支线引擎故障 |
| 动态 NPC、物品（KP-B01/B03/B04、KP-E 系列） | **未运行**：没有新增实体；当前入口的新 NPC 场景仍是占位，不能借本次交谈外推生成或物品取用能力 |
| 本轮旁白生成与发布 | **失败**：`narration=rejected`、`code=NARRATION_BODY_INVALID`。玩家可见消息只有已有开场和本次玩家输入，没有新的模型旁白 |
| 原提交精确重试、旁白恢复 | **未运行**：首个最终失败后停止，尚未到达重试阶段；不能声称零新增调用的重试已通过 |
| 事件重放 | 用当前 Room 相同的 `VNEXT_RULES_RUNTIME.replay` 离线重放原始 8 个事件，与保存的完整世界状态精确一致，额外模型调用 0 |

重放状态 hash：`sha256:eb672e86744473a0f1003989b307712d668f585d930abac9edc6313face72370`。离线审计结果保存于 `.wrangler/live-acceptance-20260914/offline-audit.json`。最初误用旧默认 replay 导出时返回 `unsupportedProfile`；改为该 Room 实际绑定的 vNext runtime 后通过，没有修改输入或事件。

## 旁白失败的直接证据与质量判定

第 5 次调用使用普通 JSON 传输，`thinking.type=enabled`、`reasoning_effort=low`、`max_tokens=5047`。原始响应为：

```text
finish_reason = length
message.content = ""
completion_tokens = 5047
completion_tokens_details.reasoning_tokens = 5047
```

直接故障边界已确认：**共享输出额度全部被思考消耗，Provider 在产生正文前截断，现役正文提取器按格式合同拒绝。** [生成预算计算](../../../app/_runtime/lib/kp/narration-vnext.ts)预留思考 token，但该预留不限制 Provider 实际思考量。对同一原始响应离线调用 `extractFrozenNarrationResponse(response, "generation")`，稳定抛出 `ModelOutputValidationError`，与实际 HTTP 的 `NARRATION_BODY_INVALID` 一致。不是网络 500、数据库未提交或独立旁白审核拒绝。不能由一个样本断言模型别名变化是根因，也不能据此直接决定提高预算或关闭思考。

对照 SPEC 0001 §12、SPEC 0015 §§7–8、SPEC 0016 §8.3，本轮没有可发布正文可供文风评价：

| 质量维度 | 结论 |
| --- | --- |
| KP-H01 通顺自然、H02 可行动、H03 声口、H04 玩家决定权、H05 不出戏 | 无新正文，材料不足；不能拿预写开场或未发布的提案台词代替验收 |
| KP-H06 结果完整与一致 | 完整体验失败：世界交谈结果已保存，玩家未收到本轮叙述；对不存在的正文不作“内容失真”判定 |
| KP-H07 连续性、H08 行动归属 | 未获得已发布的后续正文，未验收 |
| KP-H09 秘密边界、H10 恢复一致 | 未执行成对 Viewer 或实际恢复，不判通过；已提交状态未因旁白失败回滚仅是本次直接证据 |

下一项最窄检查是针对这份冻结旁白输入，核对当前实际服务模型的思考与正文输出预算合同，并设计一个有界、单变量的对照。此轮没有继续付费采样或修改生产参数；修复后仍须经过真实生成、独立审核、发布与原操作恢复，才能闭合该路径。

## 调用、时间与费用

| 调用 | 阶段 | 输入（命中 / 未命中） | 输出 | Provider 耗时 | 按官方标价估算 ¥ |
| --- | --- | --- | --- | --- | --- |
| 1 | 能力与材料选择 | 31,955（512 / 31,443） | 90 | 1.454 秒 | 0.06362648 |
| 2 | 能力选择 | 40,412（12,288 / 28,124） | 63 | 0.726 秒 | 0.05724352 |
| 3 | 社交提案填写 | 39,782（36,480 / 3,302） | 575 | 3.186 秒 | 0.01266320 |
| 4 | 受限修订 | 41,364（36,480 / 4,884） | 520 | 3.236 秒 | 0.01538720 |
| 5 | 旁白生成，截断 | 4,917（2,304 / 2,613） | 5,047 | 26.227 秒 | 0.04569416 |
| 合计 | 5 次实际调用 | 158,430（88,064 / 70,366） | 6,295 | 34.829 秒 | **0.19461456** |

官方价格于 2026-09-14 16:14:57（北京时间）核验；页面 SHA256 为 `a1602748f50a9baacd416f4b831438896ba1351160f1f9afe89a9423c4b76cf3`。本轮处于工作日高峰，Flash 每百万 tokens：命中输入 ¥0.04、未命中 ¥2、输出 ¥8。计算式为 `(88064×0.04 + 70366×2 + 6295×8) / 1000000`；思考已包含在输出，不重复计费。此数是官方标价估算，不是账户扣款凭证；运行器自身的未指定币种估算不用于人民币记账。

本次未执行整份 103 项矩阵、长团稳定性、浏览器体验、动态物品、其他 NPC、独立支线创作或恢复验收。首条链路已暴露可复现的旁白输出失败，按批次停止条件收口；剩余额度没有继续使用。
