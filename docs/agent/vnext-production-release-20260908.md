# vNext 新房默认启用：生产发布回执

日期：2026-09-08。用户明确授权 push、快速部署，并确认“包含，新房默认使用 vNext”；部署后用户再次确认“线上可用”。

## 发布结果

| 项目 | 已核验结果 |
| --- | --- |
| 源码 | `cloudflare` / `ce349be46f4e158ccac7855d34a932598213431e` |
| Cloudflare 版本 | `11a9009d-ed5f-493e-a496-a216395e5ea8`，2026-09-08 14:32:27 UTC 创建 |
| 流量 | `deployments status` 确认 100% 分配给该版本 |
| 地址 | <https://zhuwei.yinskyriver.workers.dev/>，首页 HTTP 200 |
| 新房默认 | 不提交 model 参数的正式开房请求保存当前 vNext 工作流；界面提供其已支持的 Flash 模型 |
| 持久化绑定 | 工作流原文与冻结候选精确相等，SHA256 `dcd38dff093f10dc4d32c440c1c53994e119b14dfef2bed2b88dd634a53b1c6f` |
| 既有资源 | Worker `zhuwei`；`ROOMS`/`DB`/`AI`/`ASSETS`；数据库 `f5a448fd-4224-4e52-bafb-a84cb190b618`，均保持既有目标 |
| 远端修改边界 | 未执行远端 migration、Secrets 修改、资源创建或旧房删除 |

部署包含原有 42 个已提交增量、本任务旁白/填写修复及已授权的新房默认接入。其他任务尚未完成的承诺生命周期、故事、Room/Rules 改动留在主目录，本次未纳入。

## 验证证据

- 旁白、填写、运行时绑定与活动注意力定向 Node 组首次 124/126；修正已定位的继承测试断言后，两文件最终 17/17，其余九文件 109/109。后续默认接入的绑定/拒绝检查 8/8，Provider/界面消费者八项全部由原通过六项和最终两项组成。没有改变权威校验来清洗失败。
- 初始 Room 定向组 5 passed / 45 skipped；默认接入后，正式 `ROOMS` 两种建卡后施法路径及随机恢复 3 passed / 3 skipped。最终 typecheck、部署守卫三项和 diff 检查退出码均为 0。
- 本地不设置 vNext 开关，经正常注册、默认开房、建卡、开局、fetchTable 成功，官方 D1 查询证明完整 vNext 绑定。生产执行同样路径再次成功。
- 唯一一次生产构建成功。首次 `cf:deploy` 在上传 Worker 前查询服务返回 503，退出 1，随后控制面证明旧版本仍接流量；复用同一产物执行 `wrangler deploy`，退出 0。103 个构建文件重试前后哈希完全相同，清单 SHA256 为 `cffdda202e0ab818ad84843d0f20b1dbd3a42c21aec6f2500c145f6f07124b04`。
- 新版本 `versions view` / `deployments status` 退出 0，绑定不变、100% 流量。直连首次因本机 DNS/连接失败未到 Worker；使用系统已配置的 `127.0.0.1:7897` 代理完成独立通道验证，没有修改代理配置或业务代码。

## 一次真实生产施法

正常创建的新房和三级牧师，输入“我握住圣徽，对自己施放一次一环的治愈伤口。”；默认 DeepSeek 返回 `committed / published`。

实际状态：HP `24/24 → 24/24`，一环法术位 `4 → 3`，二环仍为 `2`。正式公开旁白为：

> 你握住圣徽施放一环的治愈伤口，效果已然生效，但由于你本就满血，生命值未发生变化，仍为 24/24；这次施法消耗了 1 个 1 环法术位，剩余 3 个。

同一 submission 再次提交，公开返回结果、权威投影、Receipt 集合、Delivery 和消息逐值一致，没有重复扣除法术位。生产原始资料只存本机私有文件；生产测试账号和该验收房保留用于追溯。

一次行动预设上限七次物理调用、十分钟、¥2，未额外发起第二行动或重新采样。生产日志尾流因网络连接超时未接通，无法在这次生产证据中给出精确物理调用数、tokens 或费用，也不把返回一致写成已实测零模型调用。未读取完整生产随机 journal/replay；相关本地证据与生产公开状态证据分开记录。

## 限制与后续

- [等待重复请求的旁白状态不一致](vnext-other-filling-validation.md)尚未修复；本次治疗重复请求正常，不代表该等待问题已消失。
- 六个旧房仍在目录中，其中三个绑定旧 workflow，三个没有绑定。它们未被迁移或改写；使用新房进入 vNext。旧房退役授权仍仅限已核验旧数据，不外推到新建 vNext 房或账号资料。
- 未完成全量回归、统计稳定性、多人长期游玩或全部功能验收；本次按已授权快速发布流程验证，用户的线上可用确认不替代这些证据。
- 新建 vNext 房不得交给旧 V3 Worker 重新解释。发生问题时保留已部署协议进行前向修复，不盲目回滚旧版本。
- 推送仅针对 `cloudflare`。远端 `main` 在操作前后均为 `cf7dbddab8cfb36365734fe96c42d82456fa1d0e`；这与 AGENTS 的历史基线不同，但本任务未修改它。

完整命令、日志、候选清单、构建清单及私有生产记录位于 `/tmp/zhuwei-quick-release-20260908/`；执行审计见 [refactor-log](../refactor-log.md)。
