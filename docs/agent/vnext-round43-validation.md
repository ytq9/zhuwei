# vNext round43：旁白审核候选未通过

2026-09-06，沿用冻结材料，保留生产 v6 完整逐断言校验器。临时候选把每项 fact 的原 text 与 evidenceId 并列，说明未执行动作与物品状态的差别，并使用已证兼容的 strict required / thinking disabled；产品旁白源码没有采用候选。

预声明四例：已提交放置动作的普通实现应接受、无依据的燃烧/照明状态应拒绝、NPC 的归因发言应接受、把该发言当世界真相应拒绝。预算最多4调用、48,000输入、32,768输出、每次45秒、全批240秒、¥0.44，首预期不符停批。

首例3,137ms返回工具正文，但完整校验拒绝。原始响应存在以下问题：f2属于另一个Claim却没有该Claim证据；optional f3既未覆盖也未省略；statement添加正文没有的“背包”，并将“没有点燃这个动作”加强为“火把未被点燃”的状态，以“已放下”借证。后3例未调用，不能将它们算通过。新材料减少查表距离仍没有消除错误，因此候选不采用，也不继续同配置抽样。

玩家原意图本来含“不点燃”，普通动作实现不能一概判为新状态；这次问题包含审核自身的语义加强。候选正文有意未包含未获权威空间材料证明的“脚边”，即使通过也不能证明原round33全文合格。已更正[round37–40](vnext-round37-40-validation.md)对此的过强表述，历史失败保持不变。

实际1调用，4,728输入（1,536命中、3,192未命中）、502输出，按同日已核验官方空闲价为¥0.0071238。无未知usage，无Rules/Room提交、发布或部署。预检最初找错临时fixture目录，修正后才执行API；预检失败为0调用。

命令：`node --import tsx /tmp/zhuwei-review-candidate-43.mts --execute`，exit1。原始产物：`/tmp/zhuwei-review-candidate-43-private/`；[脱敏结果、源码与产物SHA](vnext-round43-live-evidence.json)。这是审核协议探针，不能当作完整行动或连续游戏稳定性证据。
