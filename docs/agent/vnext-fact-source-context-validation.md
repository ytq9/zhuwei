# 已展示事实的冻结绑定与依据诊断

2026-09-07，cloudflare/258caee继承工作树。真实round63第三稿使用了服务器展示且已提交的NPC痕迹，却因该记录缺known/citation/read binding被拒绝。原校验还把根/分支依据错误合并成一句原因，丢了填写位置。

同一个worldFactConstraints frame只构造一次，其typed facts已有选集逐个进入原index→rereadEntry→authorityRevisionOrHash→citationClass链路；sourceRecord为decisive、不可扩展义务，保留预算不足时完整失败。不会把事实变成观察主体、展开旁观NPC私有Knowledge，或从未来计划/任意字符串生成事实授权。隐藏事实仍仅作为原KP因果权限依据，公开感官结果和Viewer投影沿既有Rules校验。

根与sensoryEvidence/pressure/opportunity依据在原接受predicate失败点保留code、actual、constraint和精确位置。Bundle映射回原basisRefs、observe.existingFactRefs及分支字段；服务器合成字段找不到真实来源槽时不编造path。修订仍拒绝换来源、造read hash或补裁决；已有安全格式修复范围和调用次数不变。Provider/Room的结构化诊断消费者继续接收原诊断，公开错误不披露正文或其他NPC知识。

| 代表性行为 | 本地证据 |
| --- | --- |
| Rules实际形成并执行计划后的NPC trace；另一结构physicalMark | 同一freeze→observe→Rules→Viewer→replay路径接受，冻结真实版本 |
| 未来trace、异域事实、隐藏事实 | 前两者无known授权；隐藏仅authority，公开投影无隐藏正文/私有计划 |
| 缺绑定、删事实或改版本、预算不足 | 拒绝且无提交，不用Profile hash冒充动态绑定，不返回部分context |
| observe与worldInteraction根/分支依据缺授权或缺读取绑定 | 同一通用诊断，原槽和原值准确，原稿/状态不变，bridge完整传播 |
| 服务器合成basis没有原稿槽 | 保留真实原因和值，不编造字段位置 |

主树验证（同一源码状态）：

- `npx tsx --test tests/kp-vnext-private-lowering-diagnostics.test.mjs`：8/8，exit0，`/tmp/zhuwei-basis-diagnostics-node.log`。
- `npx tsx --test tests/kp-vnext-fact-source-context.test.mjs tests/kp-vnext-context-runtime.test.mjs tests/kp-vnext-observable-context.test.mjs tests/kp-vnext-world-fact-memory.test.mjs`：33/33，exit0，`/tmp/zhuwei-fact-source-final-node.log`。
- `npm run typecheck`：exit0，`/tmp/zhuwei-fact-source-final-types.log`。

事实闭包4文件逐baseline/new SHA集成、0冲突，备份`/tmp/zhuwei-fact-source-root-before`，见[清单](vnext-fact-source-context-integration.json)。root另改proposals.ts、proposal-bundle-lowering.ts及诊断测试；全部继承修改保留。

round63同稿离线取证退出0：旧冻结context仍在`proposals/0/branches/success/sensoryEvidence/0/basisRefs/1`准确拒绝；同一权威state经正式freeze新context后，完全相同模型稿通过lowering、Rules及追加replay，9事件，entities/fictionTimelines不变。原文件SHA均保持，API与持久化写入均0，证据`/tmp/zhuwei-round63-source-offline-uw87arbx/receipt.json`。这是新准备的离线因果验证，不是旧请求恢复。

round63仍按真实失败记账。本地受控通过不证明真实成功或稳定性提高；[round64](vnext-round64-validation.md)第一句通过，第二句未闭合JSON拒绝，未到第三句，来源闭包的原三句真实复验仍未完成。其他嵌套definitions/continuity没有无界补引用。NPC真实新计划、复杂Activity、多人20+及完整Goal未完成。未部署、push、commit或退役数据。

2026-09-07 [round65真实复验](vnext-round65-validation.md)：原三句全部提交发布，第3句模型真实选择observe后以已展示的前轮trace作为根/分支basis通过；18事件replay一致。事实来源闭包由离线因果证据进展为本实例真实后续引用证据，非统计稳定性证明。
