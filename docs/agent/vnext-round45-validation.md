# vNext round45：证据绑定候选的真实对照与人工缺口

2026-09-06。保持round43相同冻结材料、四个候选正文、模型、disabled thinking / strict required及语义说明；新增变化是将逐断言覆盖与证据绑定，并要求每个fact显式填写covered/omitted。目录来自原冻结Viewer材料，解码只翻译模型明确选择的同一断言内证据，然后运行原v6完整validator。产品源码未采用候选，没有Room提交或旁白发布。

原型的可选对象属性不符合现役DeepSeek strict方言，本地预检拒绝，0外部调用。临时适配为带factKey判别字段的稀疏证据数组，每个分支的证据枚举由该fact的原目录派生；没有自动补引用、补覆盖或把整段证据混合。现役strict预检四个请求均通过，最终输入保守估计6,607/6,613/4,599/4,597，均低于12k；两个结构不同的适配器行为用例验证错Claim、重复边、额外字段和旧对象表示拒绝，2/2、exit0。原型已有七组离线检查，不累计成实际模型证据。

预设最多4调用、48,000输入、32,768输出、每次45秒、全批240秒、¥0.44；首自动预期不符即停。实际四次均返回，自动接受/拒绝与预设相符：

| 样本 | 原v6完整门 | 人工核对 |
| --- | --- | --- |
| 放置动作的普通实现，明确不执行点燃 | 接受，2,864ms | f2独立证据及f3省略已正确；“取出”却只引事务已提交和行动者ID，没有具体release操作锚点，未通过人工验收 |
| 无依据的未燃烧与完全无光状态 | unsupportedClause拒绝，3,374ms | 拒绝方向正确；statement把“火把没有燃烧”改释为玩家选择不点燃，释义仍不准确 |
| NPC声称门后无人 | 接受，2,143ms | 保留说话者和声称归属，不要求其内容为世界真相 |
| 把NPC说法写成客观真相 | unsupportedClause拒绝，1,489ms | 正确识别缺少客观证据，不泄露私有动机或真假答案 |

结论是**四个自动预期匹配，但人工语义/依据验收未全通过，候选不采用**。绑定结构消除了本批此前出现的f2错链接和f3漏评，不能据此宣称真实审核稳定性或总体成功率提高。未执行点燃动作并非必然断言火把未燃烧；本批没有把玩家明确意图一概认定为KP编造状态。精确“脚边”仍未在候选中，不属于本批已验证范围。

四调用18,776输入（2,944命中、15,832未命中）、1,350输出，按同日已核验官方空闲价Decimal复算¥0.0299702，全部usage已知。累计round6–45为87 attempts/76 known usage，801,020输入/65,601输出，已知¥1.18771775–1.3018545，另11次未知费用；全部计开发验证。

命令 `node --import tsx /tmp/zhuwei-review-candidate-45.mts --execute`，exit0仅表示四个机器预期匹配，不代表人工验收通过。日志 `/tmp/zhuwei-review-candidate-45.log`，原始产物 `/tmp/zhuwei-review-candidate-45-private/`，候选及适配器 `/tmp/zhuwei-narration-review-v8/`；[请求、响应、候选源码SHA与脱敏结果](vnext-round45-live-evidence.json)。没有新增修订、重采样、改判旧批、部署或push。
