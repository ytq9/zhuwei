# Round100：承诺诊断修复后前两句直接提交，第三句两次空对象填写

2026-09-10 凌晨（空闲时段），源码 `20a1545`（承诺 terms 形状诊断逐字段定位），场景同 round82–91/98/99，本地上限 10。**首句 6 次调用直接提交并发布（填写一次成稿，无承诺无计划），第二句 4 次调用提交并发布（等待一小时）；第三句 3 次调用后 `PROPOSAL_REPAIR_EXHAUSTED / needsKp`——填写请求（估算 53,695 / 真实 49,071）返回空对象 `{}`，重发原请求（53,200 / 48,708）再次空对象。13 次调用，290,003 / 2,286 tokens，¥0.890583（峰值计）。本次待验证的承诺诊断修复没有被触发：三句草稿都没有写承诺。**

## 首句（6 次调用，已提交）

| 调用 | 用途 | 估算输入 | 真实 prompt tokens | 输出 |
| --- | --- | --- | --- | --- |
| 1 | offer（选 social） | 32,295 | 27,487 | 46 |
| 2 | 填写 → 完整草稿（directSuccess，一步 social，无承诺） | 39,675 | 34,633 | 681 |
| 3–6 | 旁白、审核各两次 | — | 1.9k–4.5k | — |

公开结果：瓦罗说原件登记在册、不能离手；要副本得说清谁要、做什么，在账台候着他可以誊给你看；反问姓名与当值处。没有承诺、没有计划，+300,000,000 微秒。gate1：wait，0 承诺 0 计划，逐项核过。

## 第二句（4 次调用，已提交）

| 调用 | 用途 | 估算输入 | 真实 prompt tokens | 输出 |
| --- | --- | --- | --- | --- |
| 7 | offer（选 passTime） | 39,789 | 35,651 | 51 |
| 8 | 填写 → passTime | 42,907 | 39,181 | 90 |
| 9–10 | 旁白、审核 | — | — | — |

时间 300,000,000 → 3,900,000,000 微秒，无活动、无到期工作。gate2：pending，coverage `waited-without-plan`。

## 第三句（3 次调用，needsKp）

| 调用 | 用途 | 估算输入 | 真实 prompt tokens | 输出 |
| --- | --- | --- | --- | --- |
| 11 | offer（选 observe + social） | 42,093 | 37,698 | 50 |
| 12 | 填写 → `{}` | 53,695 | 49,071 | 25 |
| 13 | 重发原填写请求 → `{}` | 53,200 | 48,708 | 25 |

两次空回复的原始响应都是 `finish_reason=tool_calls`、25 个输出 token、`thinking` 关闭、`max_tokens` 4000、`tool_choice` required：是模型主动给了空参数调用，不是截断，也没有 reasoning 字段。跨轮次看，空对象出现在真实 35.3k（round97）、44.5k（round98）、49.1k/48.7k（round100 第三句）；完整草稿出现在 34.6k、35.2k、39.2k、44.1k——没有干净的阈值，但本句是至今最大的填写请求，估算 53.7k 已逼近 58k 门槛。

它为什么大：这句没有点名任何 NPC，按旁观者规则冻结了全部三位在场 NPC 的决策视图和已加载知识（上下文 JSON 从首句 60k 字符涨到 85k 字符），offer 又选了 observe + social 两族，submit schema 42,912 字节。这正是用户搁置待议的"动态召回"范围：未点名的句子仍会加载每一位可见 NPC。至此，"去账台看副本"这句在相关性冻结上下文上仍未覆盖（round98 未发、round100 填不出）。

## 收尾

stop 闸门记为 `formatFailure`。`services.py shutdown` 两角色 `verifiedAbsent=true`。replay `exactState=true`，stateVersion 11，11 事件。389 项源码起止 `allEqual=true`。[机器证据](vnext-round100-live-evidence.json)。私有证据在 `/tmp/zhuwei-round100-npc-preparation/evidence`。
