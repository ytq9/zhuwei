# 部署执行日志：2026-09-22

用户于 2026-09-22 在当前任务中明确授权 push 与部署，并在知悉下文风险后确认发布 HEAD 全部提交。远端 migration **未获授权，未执行**。

## 冻结源码

| | |
| --- | --- |
| 分支 | `cloudflare` |
| 冻结 SHA | `fc586c4018738bf291dcbf305c3afd77162eca49` |
| 工作区 | `git status --porcelain --untracked-files=all` 为空 |

## 冻结门

按[发布流程](./release.md)「正式部署」一节执行：

| 门 | 结果 |
| --- | --- |
| `npm run typecheck` | 通过 |
| `npm run lint` | **未通过**，见下 |
| `node tests/run.mjs --suite node` | 1546/1546 |
| `npm run test:worker` | 345/345（46 个文件） |

### lint 未通过的实情

`eslint .` 报 86,213 个 error，其中 86,047 条来自 `.wrangler/`（本地运行状态）与 `.claude/worktrees/`（agent 工作树副本）——`eslint.config.mjs` 的 `globalIgnores` 未包含这两个 gitignore 目录。

真实源码 202 条，构成：`no-unused-vars` 157、`no-explicit-any` 31、`prefer-const` 3、`no-regex-spaces` 3、`react-hooks/set-state-in-effect` 2、`triple-slash-reference` 2。157 条 `no-unused-vars` 多数打在 `_hash`、`_kind`、`_ticketHash` 这类下划线丢弃变量上，而该写法是本仓库自身惯例，配置未设豁免。

逐文件交叉比对确认：**202 条没有一条位于本次会话改动的行上**；本次改过且带 lint 问题的三个测试文件，其 9 条均为既有 import 与丢弃变量。

另需记录：`globalIgnores` 含 `app/_runtime/**`，即 Room 编排、规则引擎、KP 适配器与归档**整体不在 lint 覆盖内**。本次改动的 `authority-store.ts`、`archive.ts`、`durable-object.ts`、`telemetry.ts`、`narration-vnext.ts` 均未被 lint 检查。

用户在知悉上述实情后裁定：带此红门部署，`set-state-in-effect` 两条部署后再查。

### 本次会话引入并已修复的唯一回归

`tests/kp/context/context-relevance.room.test.ts` 的 2 个用例在 Worker 套件中失败（`SyntaxError: Unexpected token '整'`）。根因是该文件自建布局读取器，按旧的两消息布局从 `messages[0]` 切片后解析，三消息布局下解析到规则散文。已改用共享夹具 `tests/support/fixtures/vnext-request-layout.mjs`（提交 `fc586c4`），重跑 2/2 通过，Worker 全套复跑 345/345。

遗漏原因：改布局时搜索消费者的 `grep` 加了 `head -20`，而实际文件多于 20 个。

## Push

| | |
| --- | --- |
| 命令 | `git push origin cloudflare`（非 force） |
| 区间 | `d54bc13..fc586c4` |
| 退出码 | 0 |
| 推送前 `origin/main` | `cf7dbddab8cfb36365734fe96c42d82456fa1d0e` |
| 推送后 `origin/main` | `cf7dbddab8cfb36365734fe96c42d82456fa1d0e` |

读回比对证明 `main` 未被本次操作改动。

## 部署

首次 `npm run cf:deploy` 被部署守卫拒绝：`DEPLOY_SOURCE_SHA is required in CI and cf:deploy lifecycle runs`。守卫要求该变量为 40 位小写 SHA 且与 HEAD 完全一致。**该次未进入 build，未发生任何部署。**

```bash
DEPLOY_SOURCE_SHA=fc586c4018738bf291dcbf305c3afd77162eca49 npm run cf:deploy
```

| | |
| --- | --- |
| 账号 | `Yinskyriver@gmail.com's Account`（`7aca31eae821510ea477022b0c0e0e91`） |
| Worker | `zhuwei`（既有） |
| 绑定 | `ROOMS`(DO)、`DB`=`zhuwei-dev`(D1)、`AI`、`ASSETS` —— 均为既有，未创建新资源 |
| 版本 ID | `b9ce76c3-a11e-4481-a3cc-f64901362ce9` |
| 流量 | 控制面确认 100% |
| 创建时间 | 2026-09-22T13:06:36.239Z |

### 部署记录未携带源码 SHA

`wrangler deployments list` 中本次部署的 Message 为 `-`，而此前几次部署的 Message 形如「…; source 07418f91d97af…」。`DEPLOY_SOURCE_SHA` 只被守卫消费用于校验，`cf:deploy` 不把它写入部署消息。**因此控制面记录不指明源码提交，版本 `b9ce76c3` 与 `fc586c4` 的对应关系仅由本回执建立。**

相关地，上一版部署记录的源码 `07418f91d97af0daf84ed272946b2b67d1526f4c` 在本仓库中不存在（`git cat-file -e` 失败），无法计算与本次的精确差量。按日期估算约 74 个提交、629 个文件、+19,796 −66,219。

## 发布后检查

| 入口 | 结果 |
| --- | --- |
| `GET /` | 200，25,225B，标题「烛帷｜AI 主持的多人 D&D 跑团」 |
| `GET /hall` | 200，15,580B |
| `POST /api/game`（匿名，`listMyRooms`） | 401 |

匿名 401 与 [README](../../../README.md) 「匿名访问 `/hall` 会看到登录入口，受保护 API 返回 401」一致。

## 未执行与未验证

- **远端 D1 migration 未执行**。`drizzle/` 有 14 个 migration 文件，远端应用状态本次未读取。远端 migration 需单独授权，「部署」不涵盖。若新代码依赖尚未应用的 schema，线上可能出现不匹配。
- **线上既有房间未验证**。本次区间包含 V5 归档的大量删除，以及 `VNEXT_KP_WORKFLOW_HASH` 的两次移动（当前 `sha256:412199ba`）。线上房间的 stage proof 带的是旧哈希，归档验证与 Room 输入校验会拒绝；`server.ts` 对 V5 profile 房间返回 `v3BindingRejection()`。方向有 [ADR 0028](../../adr/0028-abandon-persisted-v5-private-form-rooms.md) 支撑，但「线上已有房间在新代码下的实际表现」在部署前后均未验证。用户在知悉后确认发布。
- **外部 AI 能力未做生产探针**。本次未对生产环境发起真实模型调用，因此不声明「外部能力已恢复」。
- **lint 未通过**，见上。

## 结论

- **本地代码已验证**：typecheck、Node 1546/1546、Worker 345/345 实际通过；lint 未通过，原因与本次改动无关且已逐条核对。
- **部署已完成**：控制面确认版本 `b9ce76c3` 接收 100% 流量，代表性入口冒烟通过。
- **外部能力未声明**：未做生产模型探针。
