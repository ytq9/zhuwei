/node_modules
/.next
/.vinext
/dist
/.wrangler
/.vercel
/.playwright-cli
/output
/tsconfig.tsbuildinfo
# Drizzle snapshots are part of the migration source of truth.  Without them
# `db:generate` treats the whole existing database as a new migration.
/.env*
/.dev.vars*
/outputs
/work
*.pem
*.log
.DS_Store

# per-machine agent worktrees, never shared
.claude/worktrees/

# per-machine local preview config
.claude/launch.json
