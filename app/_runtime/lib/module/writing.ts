/**
 * 烛帷模组撰写合同（修订 7）
 *
 * 每本新案子只按这份写。类型在 schema.ts，
 * 登记进 index.ts 时会跑 assertModule。
 *
 * 核心：对话能拿到信息；关键段落双轨检定；失败不是零；
 * 旁白清楚自然；NPC 不把外乡人当自己人。
 */

export const WRITING_REVISION = 7 as const;

export const WRITING_RULES = `烛帷模组合同 修订 ${WRITING_REVISION}

# 原则（所有案子通用，不可删）
1. 问、看、听、摸先给表层。寒暄、自我介绍、请人读文件、掀开一看、把耳朵凑过去，不要先喊 DC。
2. 每条线索必须有四层：
   - talkText：免费层。玩家做了对应动作就说，并钉上线索板。不骰或骰失败，都停在这一层。
   - playerText：完整层。检定成功才给。独特载荷只放这里，用来推进剧情。
   - failText：仅物理/危险失败。写清：仍观察到什么、发生了什么后果、还能换什么方法。社交/调查双轨失败不要用惩罚。
   - hint：钉板时给玩家看的下一步提示。不剧透，能立刻去做。
3. 骰子换：对方不愿给的、被藏起来的、有风险的、同一条的更深一层。关键段落（有 dc 的线索）在免费层说完后必须 call_roll。
4. 信息失败 ≠ 零，也 ≠ 今晚锁死。物理失败（摔、伤、被看见）是真失败。
5. 冷场推进：玩家闲聊或卡住两轮，用 stallBeats 让在场势力按目标动。推动选择，不指定答案。
6. 不把完整谜底写进开场旁白。不把对话做成检定墙。不因「怕铁路」让场面僵死。
7. NPC 按目标说话。他们愿意说的 knows，问到就给。canBePersuaded 写清代价，不要「先过 DC 才肯开口」。
8. 身份门槛。玩家是外乡人时：公开场合正在发生的事（宣读、遗体、闲话）可以看、听；私人文件、钥匙、后室、承认「还有一份」、把原件交到手里——要先报上姓名/来由，或正好符合该 NPC 自己的目标。命令口气不能跳过。区别「听人念」和「把纸拿走」。
   态度只给 KP 记（待客/有名/有用/受托/敌对），不对玩家显示档名。掉档不是死局：公开层仍给，必须说明玩家还能问谁、去哪里、或等对方再开口。禁止「我不跟你说了」。
9. 不写唯一解法。角色可以死，线索可以换路拿到，不能永久无路。
10. 每人有声口。NPC 必须有 voice（用词、礼貌、态度、语速）和至少两句 lines（他会这么说，须是完整、可理解的话）。引号里去掉名字也要听得出是谁。禁止全员同一套黑话或同一套残句。每次回复只让一名主要发言者把话说完；没做事的人不要写。
11. 短团结束留 2 个续章钩，不当必须主线。
12. 旁白是现场 KP 口述，不是诗歌、预告片或谜语。禁止每段用固定小动作扫视全场收尾。

# 双轨检定（关键段落）
带 dc 的线索是「两条线」，不是墙：
- 不检定，或检定失败 → 同一个正常答案（talkText）。话题还在。
- 检定成功 → 另一个更推进剧情的答案（playerText）。
KP 必须在免费层说完后立刻停下来让玩家掷。双轨内容只供内部裁决；公开 reason 只模糊概括检定目的，不写成功、失败、答案或线索内容。
同一技能不可原地连掷；换人、换灯、换方法可以。

# 物理/危险失败（failText）
社交和调查的双轨失败不要写成惩罚离席。坠落、潜行被看见、强行撬门、爆炸才用失败文本。直接写三件事，不要用标题，不要做成固定节拍：
- 玩家仍然观察到什么（已给过的免费层不要收回）
- 发生了什么后果（必须有代价，但不能是「今晚不许再碰」）
- 还能立刻采取什么不同方法（不能是同一技能再掷一次）

# 不要写的失败
- 「你什么也没发现。」
- 「这条线索今晚不能再试。」
- 把成功信息换个说法再给一遍
- 纯机制（「你需要 14」）
- 羞辱角色；失败即剧透
- 每段旁白点名全场

# 本子结构
1. 封面：id、title、level、players、时长、调性、失败意味着什么
2. 真相页 truth：永不进玩家界面
3. 章 2–4 个，每章 3–6 个场景
4. 线索表：revealWhen、talkText、playerText、failText、hint、pointsTo、可选 dc
5. NPC：publicFace、goal、knows、doesNotKnow、hostileIf、canBePersuaded、stats、voice、lines
6. triggers：含至少一条冷场
7. stallBeats：至少 2 条具体动作
8. failures：物理失败 vs 信息失败分开写
9. banned
10. sequelHooks：至少 2 条

# 检定标尺（3 级桌）
- 一眼能做的事：不骰
- 关键段落更深一层：DC 10–13，双轨
- 对抗、撒谎、危险：DC 13–15
- 极少用 DC 16+

# 新案子检查清单
- [ ] 每条线索 talkText / playerText / failText / hint 都在
- [ ] 有 dc 的线索是双轨：失败=免费层，成功=完整层
- [ ] hint 不剧透，能立刻去做
- [ ] 至少 2 条 stallBeats
- [ ] 开场 boxedText 不泄漏 truth，用完整句，不堆氛围道具
- [ ] 每个 NPC 有 voice 和至少两句完整 lines
- [ ] banned 含：不检定墙、不点名收尾、不失败锁死
`;

export const WRITING_PRINCIPLES_FOR_KP = `模组合同原则（运行时必须遵守）：
- 问看听摸先把 talkText 写成现场能听懂的旁白，并钉线索板。
- 关键段落（线索带 dc）：免费层说完立刻 hat=call_roll，rolls 带 clueId。失败=与不骰相同的正常答案，不要惩罚、不要掐话题。成功=playerText，推进剧情。
- 检定 reason 只写「进一步确认细节」一类模糊目的，不写失败会怎样、成功会得到什么，也不复述谜底。
- 物理危险失败：说明仍观察到什么、发生了什么后果、还能换什么方法。不要用离席、拉帽、抽回文件来惩罚社交/调查失败。
- 同一线索允许换方法再试，不允许同一技能原地连掷。
- 冷场两轮：从 stallBeats 里挑一条让世界动。
- 不把 truth 写进 speech / tts / log / 线索板。
- 旁白清楚、完整。禁止每段用固定小动作扫视全场。没做事的人不要写。
- 神导等即时能力走面板，不要旁白再问一轮。
- 外乡人没报来由前，公开能看的给看；原件、钥匙、后室不要因为一句「都拿来」就交。
- 台词按 voice/lines：区别在用词和态度，不在语法残缺。每次回复只让一名主要发言者把话说完。`;
