# 烛帷 Cloudflare Worker 代理合同

本分支承载私有 GitHub 项目 `ytq9/zhuwei` 的 Cloudflare 产品 V3，当前开发版本为 `0.4.0`。产品基线为 `main` 提交 `29eb06dc009c983ad61b2d862454503e67a7f40a`；产品行为服从下述规格权威，GitHub 上游只作为当前用户决定与规格未覆盖部分的未扩展能力、中文文案、规则和视觉基线。只在 `cloudflare` 分支工作，保持远端 `main` 与 grok.me 部署不变。V3 是产品代际，0.4 是应用版本；规则、事件、投影、模组和 Profile 的 v1/v2/v5 等名称是独立协议轴，不得为统一观感改名。用户已明确放弃全部 0.4 以前的房间和可恢复房间归档；当前生产树不保留其 Adapter、fallback 或 migration，未来兼容必须另行裁定。具体边界见 `docs/adr/0013-v3-product-generation-and-repository-boundary.md`。

## 产品需求与规格权威

- `docs/specs/0001-llm-kp-responsibility-contract.md` 是烛帷的主 PRD，也是产品行为的最高文档权威。产品设计、交互、架构、Prompt、规则、测试和实现不得修改、缩小或绕过其中的行为边界。
- 其他已批准或已裁定的 SPEC 是主 PRD 的补充合同，负责细化领域行为、协议和验收；它们不得覆盖 `SPEC 0001`，也不因实现更方便、代码已经偏离或存在另一种合理设计而自动失效。
- 默认不修改已批准或已裁定的 SPEC。只有用户明确要求的产品结果与具体 SPEC 条款实质冲突时，才指出冲突、影响和拟修改条款并提出一个最小确认问题；取得用户明确确认后，才能修改该 SPEC 及其直接消费者。用户未确认时，不修改规格，也不实现与其冲突的行为。
- 草案、已被替代或未批准的 SPEC 不作为当前产品行为依据；除非当前任务明确要求整理文档，否则保留其审计用途，不主动改写历史。
- SPEC 只冻结其明示的产品行为；当 SPEC 明示不决定模型、数据结构、接口、迁移或发布方案时，不得从当前实现反推这些选择已经被冻结。
- ADR 记录当前接受的技术决策，`CONTEXT.md` 统一领域语言，测试与代码提供实现证据；它们都不能反向定义或削弱主 PRD 及已裁定补充 SPEC。ADR 可以被后续明确决定取代，代码、测试和下述实现导航都不是永久架构权威。

## 开发期任务路由

普通任务先按用户意图区分“能力开发”与“Bug 修复”，二者都属于开发期，但不共享范围判定和停止条件。

- 用户要求新增、扩展、补齐或泛化一类产品行为，现有系统尚无该能力或只有不完整切片时，走“能力开发闭环”。
- 用户报告已有行为偏离主 PRD、已裁定 SPEC、已承诺产品基线或既有正常路径时，走“Bug 修复闭环”。用户可以只描述观察到的症状；截图、错误文案、复现步骤和期望结果是可选线索。
- 一个任务同时包含新能力和回归缺陷时，先分别写明两部分的验收边界；新能力以能力开发闭环定范围，既有行为以 Bug 修复闭环防回归，不把整个任务降格为一个最窄症状。

用户使用“比如”“例如”“以…为例”或同类表达时，默认把例子视为说明需求和验收的代表性样例，不是唯一产品范围。对“支持能力 C，例如实例 E”，交付对象是 C，E 至少是一个验收样例；不得把 E 的名称、数值或专用分支当成 C 的实现。只有用户明确限定为该实例，或规格把它定义为封闭枚举时，才按单一实例实现。

只有存在两种以上合理的期望行为，并且不同选择会产生实质不同的产品结果时，才提出一个最小澄清问题；否则直接推进。

除非用户在本轮明确要求“完整回归”“发布”“部署”“远端 migration”“Git push”或“里程碑冻结”，都停留在开发期路由。任务完成、跨层或多文件修改、公共接口变化、最后一次小修以及影响面暂不明确，都不自动进入冻结门、全量测试或发布流程。构建或测试运行器本身是故障对象时，可以把对应完整命令运行一次作为原症状复现。

## 能力开发闭环

能力开发关注能力闭包、变化维度和真实产品纵切，不以最少示例数量为目标。

1. 从用户目标、主 PRD 和直接补充 SPEC 提炼一句能力合同：写明使用者、对象类别、可支持的变化、关键状态转换、权限与失败边界、持久化归属和可见结果。明确记录用户例子在其中验证哪个变化维度。
2. 阅读相关入口、直接依赖和已有测试，沿“真实输入 → 权限/状态 → 编排 → 持久化/外部依赖 → 公开投影”找到现有接缝，用代码搜索枚举修改点的直接调用者、消费者和序列化边界。
3. 在动手前建立最小代表性验收矩阵：用户给出的样例、至少一个同属该能力但结构不同的样例，以及一个最高风险的拒绝、边界或既有正常路径。结构不同指触发已声明的另一变化维度，不是任意扩展相邻功能。
4. 在单一事实源实现能力，让不同样例经过同一类型、规则、状态转换和投影路径；不为代表性样例添加名称判断、专用 schema、硬编码结果或旁路。如果交付目标是可扩展机制，完成机制及其闭包边界，不需要穷举所有领域实例。
5. 跑通代表性矩阵与直接消费者。只有用户声明的能力、变化维度和端到端事实链均闭合后才停止；未要求的相邻能力写入未覆盖范围，不自行扩展。

能力开发回执按“目标 → 能力合同与代表性矩阵 → 实现与直接消费者 → 验证证据 → 未覆盖范围”组织。

## Bug 修复闭环

Bug 修复关注恢复已有不变量和控制回归面，不借故障扩展未要求的新产品能力。

1. 阅读相关入口、直接依赖和已有测试，用最窄路径复现症状，保留能定位层级的失败请求、状态、错误或 diff。
2. 沿“输入 → 权限/状态 → 编排 → 持久化/外部依赖 → 投影”定位第一个违反不变量的位置，用一句话记录根因及证据。
3. 用代码搜索枚举修改点的直接调用者、消费者、持久化写入和公开投影，只检查实际存在的直接影响。
4. 在单一事实源做最小修复。规则问题改规则层，权限问题改服务端，持久化问题改数据层，平台问题改平台接缝；不做无关重构，不用吞错、伪造成功、备用跳转、硬编码返回或自动换模型掩盖故障。
5. 运行原失败路径、一个直接相关的正常路径和一个最高风险的直接连带路径；原失败路径已覆盖其中一项时不重复增加。

直接连带错误仅指与本次根因或被修改不变量共用同一事实源，或位于因果链相邻边界的错误：直接调用者或消费者、同一状态转换的前后态、允许/拒绝或首次/重试等对偶路径，以及数据直接经过的序列化、持久化或公开投影。仅代码位置相邻、同目录、同页面或笼统的“可能相关”不属于直接连带范围。

满足以下条件即停止扩展：原症状修复后通过；一个正常路径和一个最高风险直接连带路径通过；直接消费者已逐一判定；本级要求的检查通过；没有新的无法解释失败。未覆盖的更远风险写入回执，不以全量测试代替影响分析。定向证据仍不足时，报告缺口和建议追加的一项检查，不自行升级全量验证。

修复回执按“症状 → 根因 → 修改 → 连带检查 → 验证证据 → 未覆盖范围”组织。证据只能支持相关性时明确标为推断。

## 实现取舍

1. 对尚未发布且没有持久化数据、公共接口、现役房间或可恢复归档引用的实现，替代路径闭合后在同一修改中删除过时入口、类型、测试和样式；不保留兼容层、fallback、双写，也不为无消费者的开发态代码编写 migration。0.4 已获用户明确授权一次性退役所有更早开发房间及归档，因此旧实现和兼容路径属于本条的删除范围；该授权不外推为未来版本可自动破坏持久化合同或现有数据。
2. 选择满足当前验收条件的最简单实现，不增加预防性抽象、无实际消费者的配置层或备用路径。
3. 跨层新能力先完成一条贯穿真实输入、权限与状态、编排、持久化或外部依赖以及公开投影的最小端到端纵切，再逐步扩展；这里的“最小”限制的是无关范围和纵切数量，不得把能力合同缩成单一示例。不得为了尚未完成的复杂度拆掉当前可运行路径，替代路径闭合后则删除旧路径。
4. 前端组件按独立职责和变化原因拆分，展示组件不得实现机械规则、权限裁决或持久化事实；不按文件行数机械拆分组件。
5. 通用能力优先采用成熟、持续维护且兼容当前 Worker 与浏览器运行时的库；没有明确项目理由，不自行重写已有可靠实现。
6. 添加依赖或自行实现通用能力前，先检查 `package.json`、现有依赖、Web Platform 与 Cloudflare API 能否直接满足需求；已有能力足够时不增加新包。
7. 最小纵切可以缩小功能范围，但其事实源、持久化归属和公共合同必须符合产品、安全与数据不变量。当前架构基线默认沿用；它阻碍用户明确要求的能力时，按下述架构变更路由调整，不把能力硬塞进旧形状，也不引入第二权威、临时接缝或过渡性 fallback。
8. 当主 PRD、补充 SPEC、上游和已有测试都未规定产品或交互模式时，先参考成熟同类产品与平台惯例，再选择符合烛帷合同的已验证模式；不照搬视觉，也不因此扩大当前任务范围。

## 开发期验证

默认最多选择三类直接证据，同一源码状态下每项运行一次：

- 纯文档、注释或代理说明只检查目标段落、相关链接与约束、`git diff --check` 和最终 diff，不运行代码测试。
- Node/`.mjs` 测试直接运行目标文件或用例，例如 `npx tsx --test tests/<target>.test.mjs`；`npm run test:unit` 会展开全部 Node 测试，不用于普通开发期任务。
- Worker/Vitest 测试直接运行目标文件，例如 `npx vitest run tests/<target>.test.ts`；`npm run test:worker` 会运行全部 Worker 测试，不用于普通开发期任务。
- 只有共享或导出类型、公共签名或 DTO 改变时运行一次 `npm run typecheck`。
- 只有目标文件实际受 ESLint 配置覆盖且本次风险属于静态规则时才检查相关文件；`app/_runtime/**` 当前被忽略，不能把 ESLint 结果记为该目录的验证证据。
- 能力开发用同一个目标用例组覆盖能力合同中的代表性矩阵；至少证明用户样例和结构不同的样例走同一通用路径，并覆盖最高风险边界。跨层能力至少有一个样例贯穿真实本地接口，不用只匹配示例名称的源码断言代替行为证据。
- Bug 修复验证原失败路径、一个直接相关的成功路径和一个最高风险错误、越权、重试或恢复路径；原失败路径已覆盖其中一项时不重复增加。跨层 Bug 至多增加一条贯穿真实本地接口的成功路径和一个最高风险失败路径。
- D1 schema/持久化以 `db/schema.ts` 为源，运行 `npm run db:generate` 并逐行检查新增 migration，再在本地完成一次最小 migration 与写入—读取闭环；已生成 migration 只增不改。
- 浏览器 QA 仅在验收对象本身是视觉或真实交互，且现有确定性测试不能证明结果时执行；只检查目标路径或能力矩阵中的代表性路径，不扩展整站截图。
- 真实外部或 Workers AI 探针仅在其结果会决定不同能力设计、代码修复，或用户明确要求确认外部恢复时执行一次有界生产默认组合；首个结果仍不能区分边界时才追加一次对照，随后停止。

开发期不运行 `npm test`、全项目 Lint、production build、远端 migration、`cf:deploy` 或 Git push；只有用户显式切换阶段，或对应命令本身就是故障复现对象时例外。已有行为测试覆盖验收条件后，不增加同义源码正则、重复截图或第二套端到端测试。

## 产品不变量、当前架构与实现导航

### 产品、安全与数据不变量

- 产品代际、应用版本与规则、事件、投影、模组和 Profile 的协议版本是独立轴。机械语义只采用 SRD 5.1 / D&D 5e 2014；房间由创建时固定的完整 runtime manifest 解释，已持久化标识不得原地改名或改义，也不得混入 2024/5.5e。
- 玩家拥有角色意图，LLM/KP 在故事锚点与固化事实内拥有叙事权威，Rules 拥有机械权威，权威状态拥有正史写权。所有进入因果链的事实必须经过同一机械验证、原子提交和按 Viewer 投影；页面、API、AI Adapter、归档或测试都不得成为第二条裁决或活跃状态路径。
- 活跃房间只有一个状态权威；D1 中的目录、静态资料、派生索引和可重建归档不参与活跃裁决。秘密只进入获授权的服务端上下文与 Viewer 投影，不因日志、错误、候选、缓存或恢复旁路泄漏。
- 身份必须来自服务端验证的可信会话。密码使用平台支持的单向 KDF 与随机盐，会话 token 只保存摘要；生产 cookie 至少使用 `HttpOnly`、`Secure`、`SameSite=Lax`。所有写操作验证成员身份及本次涉及的房主、队长、地点与回合权限，开发假身份和客户端身份头不成为生产身份来源。
- Google/X OAuth 只在回调、客户端配置和 Secrets 全部存在并验证后启用；密钥只通过 Worker Secret 管理。远端资源创建、远端 migration、部署和 Git push 继续需要用户在本轮明确授权。
- 保留开房、席位、建卡、语音、线索与日志、装备与职业资源、地点/时钟、组队、休整、战斗和逐地点协调等上游能力。个人合法行动不经队长审批；个人移动或休整可原子离队，队长只组织整队移动。

### 当前架构基线（默认沿用，可被明确取代）

- 当前生产目标是现有 Cloudflare Worker `zhuwei`、D1 binding `DB` 和 `wrangler.jsonc`；每个房间的 SQLite Durable Object 是 Room Authority 的生产 Adapter。当前系统由 Room Action 与 Rules 两个深 Module 承载，Rules 的现役公共 Interface 是 `step / project / replay`。这些选择默认沿用，但不是产品行为本身；满足下述变更条件时可由新增或取代 ADR 调整。
- 未明确改变平台时，继续排除已退役的 TanStack/Vercel Node、Better Auth/GPT Sites、PGLite/Postgres、Sites、`.vercel/output` 和旧 Node 入口。新 Worker、新持久化资源或外部平台不是永久禁区，但会改变远端拓扑、成本或数据责任，必须先说明影响、取得用户明确确认并记录 ADR，随后才能创建或接入。
- 模块求值必须确定、无 I/O，且不持有影响正确性的跨请求可变业务状态。随机数、`fetch`、数据库访问、定时器和读取密钥后的副作用发生在请求处理期间；无 I/O 的不可变注册表、已编译描述和纯初始化对象可以留在模块作用域。
- 架构基线阻碍用户明确要求的能力时，先区分受影响的是产品不变量还是实现选择，再阅读直接 ADR、入口、消费者和持久化边界。若替代方案不改变产品结果、持久化兼容、安全姿态、远端资源或外部成本，可在任务内直接重构并新增或取代 ADR；触及任一前述实质变化时，只提出一个最小确认问题。替代路径闭合后同步实现导航、直接消费者和验证门。
- 测试和模块门是当前 ADR 的实现证据，不是拒绝架构演进的理由。取代 ADR 时更新或删除只锁定旧路径、文件名、表名、算法或内部结构的断言，同时保留产品不变量和公开行为的验收证据。

### 当前实现导航（信息性，不是架构合同）

- 修改前以仓库、`package.json`、`wrangler.jsonc` 和代码搜索核实真实入口。当前导航为：页面、API 与运行时在 `app/`，主要部署运行时在 `app/_runtime/`，Worker 入口在 `worker/index.ts`，D1 schema 与只增 migration 在 `db/`、`drizzle/`，现役开发/发布工具在 `tools/`、`cloudflare/`。
- 当前注册的 Rules/runtime/Profile 标识以生产 Registry 为准；schema、表名与归档职责以 `db/schema.ts` 为准；认证入口、KDF 和 cookie 参数以现役认证源码为准。不要从本文件复制这些易变实现值，也不要因这里的导航与源码不一致而恢复旧路径。
- 旧平台源码只存在于已核验的私有归档分支，不回到 V3 工作树。确需恢复其中能力时提取产品行为并用当前架构实现；只有明确取代平台 ADR 后才恢复相应生产入口。

## 文档读取路由

- 恢复已经明确的产品行为时，先定位 `SPEC 0001` 的对应条款或 A–O 验收场景，再只读一个直接相关的补充 SPEC、入口、直接依赖和已有测试；不默认遍历全部规格或 ADR。若 `SPEC 0001` 未规定该细节，再以上游和已裁定补充 SPEC 补足。
- 开发新能力时，先定位 `SPEC 0001` 中约束该能力目标和边界的条款，再读取一个直接相关的补充 SPEC、现有端到端入口、直接依赖和已有测试；先形成能力合同与代表性矩阵，再决定实现范围。
- 当新增或修改 LLM/KP 叙事权威、自由行动、动态世界、NPC、危险、失败、节奏、聚光灯、故事收束或其他预期产品合同时，完整阅读 `docs/specs/0001-llm-kp-responsibility-contract.md`，并只读直接相关的补充 SPEC。
- 用户要求与已裁定 SPEC 冲突时，按“产品需求与规格权威”先确认，不得静默修改规格、测试或实现来制造表面一致。确认修改后，同步规格索引、直接引用、验收场景和直接测试；不顺带重写无关 SPEC。
- 当修改通用规则的预期合同或架构时，除主 PRD 和直接相关补充 SPEC 外，阅读根目录 `CONTEXT.md` 与直接相关 ADR；规则测试统一穿过现役 Rules 公共 Interface（当前为 `step` / `project`），若该 Interface 被取代则同步直接消费者和测试，生产编排始终不得建立第二套裁决路径。
- 现有封闭 DSL 是迁移现状，不是最终产品边界。新设计仍须让动态事实通过同一机械与状态权威验证、固化和投影；耗时动作先成为 Activity，效果在虚构时间完成后落地。

## 执行日志

`docs/refactor-log.md` 是本分支唯一执行日志，但开发任务不完整读取历史日志；需要历史证据时先用关键词搜索并只读相关段落。

开发期修改在结束前追加一条紧凑记录。能力开发记录日期、目标与能力合同、代表性矩阵、修改文件与直接消费者、实际运行的定向检查及退出码、未覆盖范围；Bug 修复记录日期、症状与根因、修改文件、连带检查、实际运行的定向检查及退出码、未覆盖范围。探索性只读命令和未改变结论的重试不逐条记录。只读调查无需记录；结论一旦驱动修改，随该修改记录。migration、部署、push、并行集成或未集成尝试才使用包含基线、外部操作、commit、冲突和处置的完整审计格式。

## 并行与发布路由

一个单一能力纵切或一个 Bug 及其直接影响默认由当前代理在当前 worktree 完成。只有至少两个可独立验收、修改路径不重叠且能产生实际并行收益的范围，才完整读取 `docs/agent/parallel.md` 并启用隔离 Worker。

只有用户本轮明确要求完整回归、发布、部署、远端 migration、Git push 或里程碑冻结时，才完整读取 `docs/agent/release.md` 并切换阶段。“修好”“完成”“检查一下”或普通任务交付不构成发布授权。
