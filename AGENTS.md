# 烛帷 Cloudflare Worker 代理合同

本分支承载私有 GitHub 项目 `ytq9/zhuwei` 的 Cloudflare 产品 V3。产品基线为 `main` 提交 `29eb06dc009c983ad61b2d862454503e67a7f40a`；产品行为服从下述规格权威，GitHub 上游只作为当前用户决定与规格未覆盖部分的未扩展能力、中文文案、规则和视觉基线。只在 `cloudflare` 分支工作，保持远端 `main` 与 grok.me 部署不变。V3 是产品代际，不得把既有房间的版本化规则、事件或投影静默改名为 v3；具体边界见 `docs/adr/0013-v3-product-generation-and-repository-boundary.md`。

## 产品需求与规格权威

- `docs/specs/0001-llm-kp-responsibility-contract.md` 是烛帷的主 PRD，也是产品行为的最高文档权威。产品设计、交互、架构、Prompt、规则、测试和实现不得修改、缩小或绕过其中的行为边界。
- 其他已批准或已裁定的 SPEC 是主 PRD 的补充合同，负责细化领域行为、协议和验收；它们不得覆盖 `SPEC 0001`，也不因实现更方便、代码已经偏离或存在另一种合理设计而自动失效。
- 默认不修改已批准或已裁定的 SPEC。只有用户明确要求的产品结果与具体 SPEC 条款实质冲突时，才指出冲突、影响和拟修改条款并提出一个最小确认问题；取得用户明确确认后，才能修改该 SPEC 及其直接消费者。用户未确认时，不修改规格，也不实现与其冲突的行为。
- 草案、已被替代或未批准的 SPEC 不作为当前产品行为依据；除非当前任务明确要求整理文档，否则保留其审计用途，不主动改写历史。
- ADR 记录技术决策，`CONTEXT.md` 统一领域语言，测试与代码提供实现证据；它们都不能反向定义或削弱主 PRD 及已裁定补充 SPEC。发现冲突时，先把它视为下位文档或实现缺陷，而不是为通过测试去修改规格。

## 默认模式：开发期 Bug 快修

用户可以只描述观察到的症状。代理主动定位入口、建立最短复现，并依据产品基线、规格、测试和上下文判断期望行为。截图、错误文案、复现步骤和期望结果是可选线索，不是开始修复的前置条件。

只有存在两种以上合理的期望行为，并且不同选择会产生实质不同的产品结果时，才提出一个最小澄清问题；否则直接推进。

除非用户在本轮明确要求“完整回归”“发布”“部署”“远端 migration”“Git push”或“里程碑冻结”，所有任务均按开发期 Bug 快修处理。任务完成、跨层或多文件修改、公共接口变化、最后一次小修以及影响面暂不明确，都不自动进入冻结门、全量测试或发布流程。构建或测试运行器本身是故障对象时，可以把对应完整命令运行一次作为原症状复现。

## 开发修复闭环

1. 阅读相关入口、直接依赖和已有测试，用最窄路径复现症状，保留能定位层级的失败请求、状态、错误或 diff。
2. 沿“输入 → 权限/状态 → 编排 → 持久化/外部依赖 → 投影”定位第一个违反不变量的位置，用一句话记录根因及证据。
3. 用代码搜索枚举修改点的直接调用者、消费者、持久化写入和公开投影，只检查实际存在的直接影响。
4. 在单一事实源做最小修复。规则问题改规则层，权限问题改服务端，持久化问题改数据层，平台问题改平台接缝；不做无关重构，不用吞错、伪造成功、备用跳转、硬编码返回或自动换模型掩盖故障。
5. 运行原失败路径、一个直接相关的正常路径和一个最高风险的直接连带路径；原失败路径已覆盖其中一项时不重复增加。

直接连带错误仅指与本次根因或被修改不变量共用同一事实源，或位于因果链相邻边界的错误：直接调用者或消费者、同一状态转换的前后态、允许/拒绝或首次/重试等对偶路径，以及数据直接经过的序列化、持久化或公开投影。仅代码位置相邻、同目录、同页面或笼统的“可能相关”不属于直接连带范围。

满足以下条件即停止扩展：原症状修复后通过；一个正常路径和一个最高风险直接连带路径通过；直接消费者已逐一判定；本级要求的检查通过；没有新的无法解释失败。未覆盖的更远风险写入回执，不以全量测试代替影响分析。定向证据仍不足时，报告缺口和建议追加的一项检查，不自行升级全量验证。

修复回执按“症状 → 根因 → 修改 → 连带检查 → 验证证据 → 未覆盖范围”组织。证据只能支持相关性时明确标为推断。

## 实现取舍

1. 对尚未发布且没有持久化数据、公共接口、现役房间或可恢复归档引用的实现，替代路径闭合后在同一修改中删除过时入口、类型、测试和样式；不保留兼容层、fallback、双写，也不为无消费者的开发态代码编写 migration。此规则不授权破坏已裁定 SPEC、已发布版本化合同或现有数据。
2. 选择满足当前验收条件的最简单实现，不增加预防性抽象、无实际消费者的配置层或备用路径。
3. 跨层新能力先完成一条贯穿真实输入、权限与状态、编排、持久化或外部依赖以及公开投影的最小端到端纵切，再逐步扩展；不得为了尚未完成的复杂度拆掉当前可运行路径，替代路径闭合后则删除旧路径。
4. 前端组件按独立职责和变化原因拆分，展示组件不得实现机械规则、权限裁决或持久化事实；不按文件行数机械拆分组件。
5. 通用能力优先采用成熟、持续维护且兼容当前 Worker 与浏览器运行时的库；没有明确项目理由，不自行重写已有可靠实现。
6. 添加依赖或自行实现通用能力前，先检查 `package.json`、现有依赖、Web Platform 与 Cloudflare API 能否直接满足需求；已有能力足够时不增加新包。
7. 最小纵切可以缩小功能范围，但其事实源、持久化归属、公共合同和模块边界必须符合长期架构；不引入明知以后需要替换的第二权威、临时接缝或过渡性 fallback。
8. 当主 PRD、补充 SPEC、上游和已有测试都未规定产品或交互模式时，先参考成熟同类产品与平台惯例，再选择符合烛帷合同的已验证模式；不照搬视觉，也不因此扩大当前任务范围。

## 开发期验证

默认最多选择三类直接证据，同一源码状态下每项运行一次：

- 纯文档、注释或代理说明只检查目标段落、相关链接与约束、`git diff --check` 和最终 diff，不运行代码测试。
- Node/`.mjs` 测试直接运行目标文件或用例，例如 `npx tsx --test tests/<target>.test.mjs`；`npm run test:unit` 会展开全部 Node 测试，不用于普通快修。
- Worker/Vitest 测试直接运行目标文件，例如 `npx vitest run tests/<target>.test.ts`；`npm run test:worker` 会运行全部 Worker 测试，不用于普通快修。
- 只有共享或导出类型、公共签名或 DTO 改变时运行一次 `npm run typecheck`。
- 只有目标文件实际受 ESLint 配置覆盖且本次风险属于静态规则时才检查相关文件；`app/_runtime/**` 当前被忽略，不能把 ESLint 结果记为该目录的验证证据。
- API、权限或交互修复验证原失败路径、一个成功路径和一个最高风险错误、越权或恢复路径。
- D1 schema/持久化以 `db/schema.ts` 为源，运行 `npm run db:generate` 并逐行检查新增 migration，再在本地完成一次最小 migration 与写入—读取闭环；已生成 migration 只增不改。
- 跨层修复增加至多一条贯穿真实本地接口的成功路径和一个最高风险失败路径，仍不自动全量。
- 浏览器 QA 仅在症状本身是视觉或真实交互，且现有确定性测试不能证明修复时执行；只检查目标路径，不扩展整站截图。
- 真实外部或 Workers AI 探针仅在其结果会决定不同代码修复，或用户明确要求确认外部恢复时执行一次有界生产默认组合；首个结果仍不能区分边界时才追加一次对照，随后停止。

开发期不运行 `npm test`、全项目 Lint、production build、远端 migration、`cf:deploy` 或 Git push；只有用户显式切换阶段，或对应命令本身就是故障复现对象时例外。已有行为测试覆盖验收条件后，不增加同义源码正则、重复截图或第二套端到端测试。

## 产品与权威边界

- 页面、API、当前规则与服务端实现位于 `app/`；部署运行时集中在 `app/_runtime/`。Worker 入口为 `worker/index.ts`；D1 位于 `db/` 与 `drizzle/`；现役开发/发布门位于 `tools/` 与 `cloudflare/`。旧平台源码只存在于已核验的私有归档分支，不回到 V3 工作树。
- 平台迁移只替换 TanStack/Vercel Node、Better Auth/GPT Sites、PGLite/Postgres 和 Node API 接缝；目标仍是既有 Worker `zhuwei`、既有 D1 binding `DB` 和 `wrangler.jsonc`。Sites、Vercel、`.vercel/output`、新 Worker 与新持久化资源不属于交付路径。
- 模块全局作用域只允许声明、纯常量和纯函数。随机数、fetch、数据库初始化、定时器、密钥读取后的副作用和其他 I/O 发生在请求处理期间。产物不得包含 Better Auth、PGlite、pg 或 Vercel 运行入口。
- 产品代际是 V3，当前已发布的新房机械解释器仍是精确固定的 `dnd5e-2014-srd5.1-authoritative-v2`；两者不是同一个版本轴。机械规则只采用 SRD 5.1 / D&D 5e 2014，房间通过完整 runtime manifest 固定版本，禁止混入 2024/5.5e。
- 叙事由 LLM/KP 在故事锚点与固化事实内负责；机械由 `app/_runtime/lib/rules` 决定；活跃状态由每房间 SQLite Durable Object 原子保存；服务端从可信登录验证权限；秘密内容只留在 Worker。
- 所有 v2 机械裁决穿过 `step(module, state, command)`，玩家快照穿过 `project(module, state, viewer)`。不得把 v2 状态另写回旧 D1 `game_states` 形成第二权威；`room_event_archive` 只追加可重建归档。
- 独立 Worker 身份来源是 `auth_users` 与 `auth_sessions`。密码使用 Web Crypto PBKDF2-SHA256 与随机盐；会话 cookie 为 `HttpOnly`、`Secure`、`SameSite=Lax`，D1 只保存 token 摘要。`app/chatgpt-auth.ts` 从可信会话取得 user id；GPT Sites 身份头和开发假用户不是身份来源。所有写操作必须验证成员身份及本次涉及的房主、队长、地点与回合权限。
- Google/X OAuth 仅在回调、客户端配置与 Secrets 全部存在并验证后启用。密钥只通过 Worker Secret 管理；未获用户明确确认时不创建远端资源、不执行远端 migration、不部署、不推送。

保留开房、席位、建卡、语音、线索与日志、装备与职业资源、地点/时钟、组队、休整、战斗和逐地点协调等上游能力。个人合法行动不经队长审批；个人移动或休整可原子离队，队长只组织整队移动。

## 文档读取路由

- 恢复已经明确的产品行为时，先定位 `SPEC 0001` 的对应条款或 A–O 验收场景，再只读一个直接相关的补充 SPEC、入口、直接依赖和已有测试；不默认遍历全部规格或 ADR。若 `SPEC 0001` 未规定该细节，再以上游和已裁定补充 SPEC 补足。
- 当新增或修改 LLM/KP 叙事权威、自由行动、动态世界、NPC、危险、失败、节奏、聚光灯、故事收束或其他预期产品合同时，完整阅读 `docs/specs/0001-llm-kp-responsibility-contract.md`，并只读直接相关的补充 SPEC。
- 用户要求与已裁定 SPEC 冲突时，按“产品需求与规格权威”先确认，不得静默修改规格、测试或实现来制造表面一致。确认修改后，同步规格索引、直接引用、验收场景和直接测试；不顺带重写无关 SPEC。
- 当修改通用规则的预期合同或架构时，除主 PRD 和直接相关补充 SPEC 外，阅读根目录 `CONTEXT.md` 与直接相关 ADR；规则测试统一穿过 `step` / `project`，生产编排不得建立第二套裁决路径。
- 现有封闭 DSL 是迁移现状，不是最终产品边界。新设计仍须让动态事实通过同一机械与状态权威验证、固化和投影；耗时动作先成为 Activity，效果在虚构时间完成后落地。

## 执行日志

`docs/refactor-log.md` 是本分支唯一执行日志，但开发任务不完整读取历史日志；需要历史证据时先用关键词搜索并只读相关段落。

开发快修在结束前追加一条紧凑记录：日期、症状与根因、修改文件、实际运行的定向检查及退出码、剩余限制。探索性只读命令和未改变结论的重试不逐条记录。只读调查无需记录；结论一旦驱动修改，随该修改记录。migration、部署、push、并行集成或未集成尝试才使用包含基线、外部操作、commit、冲突和处置的完整审计格式。

## 并行与发布路由

一个 Bug 及其直接影响默认由当前代理在当前 worktree 完成。只有至少两个可独立验收、修改路径不重叠且能产生实际并行收益的范围，才完整读取 `docs/agent/parallel.md` 并启用隔离 Worker。

只有用户本轮明确要求完整回归、发布、部署、远端 migration、Git push 或里程碑冻结时，才完整读取 `docs/agent/release.md` 并切换阶段。“修好”“完成”“检查一下”或普通任务交付不构成发布授权。
