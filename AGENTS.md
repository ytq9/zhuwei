# 烛帷 Cloudflare Worker 代理合同

本分支是私有 GitHub 项目 `ytq9/zhuwei` 的 **等价 Cloudflare 迁移**。产品基线为 `main` 提交 `29eb06dc009c983ad61b2d862454503e67a7f40a`；GitHub 上游是行为、中文文案、规则和视觉的唯一权威。只在 `cloudflare` 分支工作，保持 `main` 与 grok.me 部署不变。

## 执行日志与并行协调

`docs/refactor-log.md` 是本分支唯一执行日志。任何代码、测试、文档、配置、migration、部署状态或 Git 交付的修改，在任务结束前都必须有对应记录；只读调查无需伪造修改记录，但其结论一旦驱动修改，必须随该修改记录。日志只写实际执行事实和脱敏证据，至少包括日期/任务、基线或来源提交、需求或症状、根因或决策、修改文件或外部操作、验证命令及退出码、结果提交、冲突和剩余限制。失败、放弃或尚未集成的尝试也按真实状态记录，不得只记成功。

并行工作采用“一名协调代理 + 多个隔离 Worker”模型：

1. 协调代理是需求、公共接口、验收、任务依赖、集成、执行日志、migration、部署和推送的唯一负责人。先固定共享规格、接口、验收与可恢复基线，再启动并行写入。
2. 新问题先分流：与在途修改独立且文件所有权不重叠的，创建独立 Fork/Worktree；触及相同文件或公共接口的，先做只读诊断或排队；依赖在途修复的，等待 checkpoint 后从该提交启动；共享远端 D1、Durable Object、Worker 或本地服务端口的操作必须串行。
3. Worker 只能修改派工明确列出的路径，从指定基线开始，运行指定验证，并返回“症状/需求 → 根因/决策 → 修改 → 证据 → 剩余限制”及可收回的 commit SHA。Worker 不自行修改共享规格/接口、`AGENTS.md` 或 `docs/refactor-log.md`，也不执行 migration、部署或 Git push。
4. 并行期间由协调代理单写 `docs/refactor-log.md`。每个 Worker 以结构化回执提交日志素材；协调代理在收回或判定不集成时逐项写入任务、基线、文件、命令/退出码、commit、冲突与处置，避免 Worktree 并发编辑日志。
5. 协调代理按依赖顺序收回改动，逐项审查 diff、处理冲突并运行受影响范围的组合回归；只有达到冻结门条件时才运行全量回归。缺少 Worker 回执、尚未写入执行日志或未说明集成状态的修改，不得提交、发布或宣告完成。

## 通用规则 v2

用户已批准在 `cloudflare` 分支演进通用规则与房间协调架构。上游仍是未涉及能力和中文文案的基线；`app/_runtime/lib/rules/`、模组结构化世界定义、虚构时间、AI 意图/叙述接缝及 Room Durable Object 是本分支的权威扩展，不得为追求逐行等价而移除。规则机械只采用 SRD 5.1 / D&D 5e 2014；房间通过 `ruleset_version` 固定版本，禁止混入 2024/5.5e。保持远端 `main` 不变，新规则只通过版本化房间启用。

涉及 LLM/KP 叙事权威、自由行动裁决、动态世界生成、敌人/危险、信息与传闻、剧情推动、NPC 行为、失败、节奏、聚光灯或故事收束时，必须完整阅读 `docs/specs/0001-llm-kp-responsibility-contract.md`。该规格是这些产品行为的权威；当前封闭 DSL、旧 ADR 和现有代码与其冲突之处属于待设计的实现差距，不得用现状反向缩小规格。

修改通用规则前先读根目录 `CONTEXT.md` 与 `docs/adr/`。规则测试统一穿过 `step` / `project` 接口；生产编排不得建立第二套裁决路径。新增 Durable Object 代码和本地 Wrangler migration 属于开发范围，远程创建、迁移或发布仍须用户明确确认。

## 平台边界

迁移只替换平台接缝：

- TanStack/Vercel Node 入口 → Vinext App Router 与 Cloudflare ESM Worker；
- Better Auth/GPT Sites 身份 → D1 + Web Crypto 邮箱密码会话；
- PGLite/Postgres → 现有 Cloudflare D1，绑定名 `DB`；
- Node 运行时 API → Worker Web API。

目标是现有 Worker `zhuwei` 和 `https://zhuwei.yinskyriver.workers.dev`。`wrangler.jsonc` 是唯一部署配置。Sites、Vercel、`.vercel/output`、新 Worker 和新持久化资源不属于交付路径。

## 运行入口

- 页面、API、当前规则与服务端实现：`app/`；部署运行时副本集中在 `app/_runtime/`。
- D1：`db/schema.ts`、`db/index.ts`、`drizzle/` 与 `app/_runtime/lib/db.ts`。
- Worker：`worker/index.ts`，默认导出 `fetch(request, env, ctx): Promise<Response>`。
- `src/`、`server/`、`scripts/`、`migrations/` 是原 Grok/TanStack 实现，仅供上游考据，不进入生产构建或线上入口。

模块全局作用域只允许声明、纯常量和纯函数。随机数、fetch、数据库初始化、定时器、密钥读取后的副作用和其他 I/O 必须发生在请求处理期间。产物不得包含 Better Auth、PGlite、pg 或 Vercel运行入口。

## 产品与权威边界

保留上游完整能力：开房/入席/离席/请离/房主转移、9 步三级建卡、语音、线索与日志、装备和职业资源、分头地点/时钟、组队、休整投票、战斗和逐地点并发协调。通用规则 v2 中个人合法行动不经队长审批；个人移动或休整可原子离队，队长只组织整队移动。

- **叙事权威**：LLM/KP 在故事锚点与已固化事实内判断自由行动的可行性、动态创造世界内容、扮演 NPC 并推动世界；其完整职责以 `docs/specs/0001-llm-kp-responsibility-contract.md` 为准。
- **机械权威**：`app/_runtime/lib/rules` 的 TypeScript 内核决定 5e 数值、资源、骰子、战斗、时间和权限，并验证 KP 提案的机械合法性；规则内核不决定故事应当多危险或沿哪条路径发展。
- **状态权威**：每房间 SQLite Durable Object 原子保存活跃状态与事件，D1 只保留身份、目录、静态人物卡和可重建归档；KP 动态创造的内容进入因果链后必须通过权威状态固化，不得只留在 Prompt。
- **服务端权威**：客户端提交意图并轮询公开投影；写操作从可信登录取得 user id，再验证成员、房主、队长、地点与回合权限。
- **秘密边界**：模组 `truth`、未公开线索、内部 flags 和模型上下文只留在 Worker。

在后续实现规格明确修改接口前，所有 v2 机械裁决仍必须穿过 `step(module, state, command)`，所有玩家快照仍必须穿过 `project(module, state, viewer)`；不得把 v2 资源、物品、战斗、休整或位置另写回旧 D1 `game_states` 作为第二权威。D1 `room_event_archive` 只追加保存已提交事件，丢失后可从房间 DO 重建。

现有模组的封闭 DSL 是迁移期间的实现现状，不是最终产品边界。后续设计必须允许 KP 在故事锚点内动态形成场景、通路、物品、敌人、危险、线索取得方式、NPC 行动和收束，同时让新事实通过同一机械与状态权威验证、固化和投影；不得以开放叙事为由建立第二套状态真相。耗时动作先成为 Activity，效果只在虚构时间完成后落地；中断必须保留未发生的效果。拍只调度镜头且最多相差三拍，不代替轮、分钟、小时、短休一小时或长休八小时。

## 身份与资源

独立 Worker 的身份来源是 `auth_users` 与 `auth_sessions`。密码通过 Web Crypto PBKDF2-SHA256 加随机盐派生；会话 cookie 是 `HttpOnly`、`Secure`、`SameSite=Lax`，D1 只保存 token 摘要。随机盐和 token 只能在注册或登录请求内生成。`app/chatgpt-auth.ts` 从可信会话取得 user id；GPT Sites 身份头和 development 假用户都不是身份来源。

Google/X OAuth 只有在回调、客户端配置和 Wrangler secrets 全部存在并验证后才可启用；缺少配置时界面明确标为待配置。身份变更必须覆盖注册、错误密码、会话恢复、登出撤销、匿名 401 与登录后开房闭环。

部署前先运行 `npx wrangler whoami`。登录由用户在浏览器完成，不索取或记录 API Token。读取现有 Worker/D1 配置后，把已存在数据库的真实 id 写入 `wrangler.jsonc`；未获用户确认时不创建 D1、KV、R2、队列或其他资源。密钥只通过 `wrangler secret` 配置。

## 根因驱动

故障修复先建立因果链，再动代码：

1. 用最窄路径复现症状，保留能定位层级的事实，例如失败请求、状态快照、错误堆栈、数据库结果或相关 diff。
2. 沿“输入 → 权限/状态 → 服务端编排 → 持久化/外部依赖 → 返回投影”追到第一个违反不变量的位置，用一句话写明根因及证据。
3. 在该位置的单一事实源修复。确定性规则改规则层，权限改服务端，持久化改 D1 层，平台故障改平台接缝；界面只负责呈现真实状态与错误。
4. 修复范围只覆盖根因及其直接影响。保留无关代码和上游行为，不顺手重构，不用吞错、伪造成功、备用跳转、硬编码返回或自动换模型掩盖问题。
5. 外部网络、浏览器会话、Workers AI 或平台控制面疑似为根因时，先用本地确定性测试验证超时、失败映射和状态不变，再运行一次使用生产默认组合且不超过生产超时的代表性真实探针；只有首个结果仍不能区分应用与外部边界时，才追加一次独立通道或对照探针。两次证据落在同一外部边界后即停止并记录恢复条件。只有本次验收明确要求兼容性时才逐项探测模型、参数和调用方式。分别报告本地代码状态与外部能力状态。

修复回执按“症状 → 根因 → 修改 → 证据 → 剩余限制”组织。若证据只能支持相关性而不能确认因果，明确标为推断。

## 修改顺序

1. 先读相关入口及其直接依赖，列出本次涉及的规则、身份、权限、数据与秘密不变量。
2. 从权威内核向外修改：`app/_runtime/lib/dnd|kp|module` → 服务编排 → D1/API → 页面。
3. schema 变更以 `db/schema.ts` 为源，运行 `npm run db:generate` 并逐行检查新迁移；已生成迁移只增不改。
4. Cloudflare 变更先只读核对账号、现有 Worker、绑定与迁移状态。发布只更新现有 `zhuwei` 及其现有 `DB`，并保持密钥位于 Worker Secret。
5. 按下述风险等级取得最小充分证据；证据达到停止条件后交付或发布。

## 最小充分验证

验证以能证伪本次根因和证明直接影响为准，分为开发闭环与冻结门。实现、审查和修复期间只运行受影响的定向检查；默认选择不超过三类关键检查，只有现有证据仍不能定位失败边界或覆盖验收条件时才扩展，并在执行前说明原因。

- 文档、注释或 `AGENTS.md`：检查目标段落、`git diff --check` 和最终 diff 即完成。
- 纯规则或局部 TypeScript：运行最相关测试；只有公共类型或接口发生变化时补 `npm run typecheck`，只有改动触及静态规则时才对相关文件运行 ESLint。
- API、权限或交互：复现原失败路径，再验证一个成功路径和一个与本次最高风险直接相关的错误、越权或恢复路径。
- D1 schema/持久化：检查生成 SQL，在对应开发环境完成一次最小 migration 与写入—读取闭环；正式环境 migration 只在获授权的发布阶段执行，不重复创建数据库或测试资源。
- 跨层改造：组合上述直接相关检查，再增加至多一条贯穿真实接口的成功路径和一个最高风险失败路径。跨层本身不自动触发全量测试。

冻结门只在依赖或 lockfile、构建或测试运行器配置、影响面无法可靠界定的公共权威接口发生变化，准备正式部署或里程碑交付，或用户明确要求完整回归时触发。代码审查与全部定向修复收口后，才形成冻结候选；在同一冻结候选上运行一次 `npm run typecheck`、`npm run lint`、`npm test`。仅在依赖未安装或 lockfile/manifest 变化时运行 `npm ci`。

全量检查失败后，先定向复现失败用例并完成本轮全部实现、审查与修复，再把修复合并为一个冻结候选统一重跑最终门。已通过的检查只在其覆盖的生产代码、测试或 fixture、依赖、构建或运行器配置发生变化时失效；只修改执行日志或说明性文档不使代码验证失效。无法可靠界定影响面时升级到冻结门。

命令按包含关系去重；当前 `npm test` 已包含 production build，不再单独预跑构建。已有行为测试覆盖验收条件时，不再增加同义源码正则、重复截图或第二套端到端测试。浏览器截图/DOM QA 仅在用户明确要求时执行。

发布后用一个代表性入口做冒烟检查，并用 Cloudflare 部署状态确认版本。若请求在到达 Worker 前发生 DNS、TCP 或本地网络超时，换一个独立通道复核一次；仍不可达时停止重试，报告传输层限制，不把它误判为应用回归，也不为此修改业务代码。

## 完成门

任务分别报告“本地代码已验证”“部署已完成”“外部能力已恢复”。非发布任务取得对应风险等级的定向证据即可交付；外部探针失败时，可以交付已验证的代码和安全失败行为，但必须明确外部能力尚未恢复，不得宣称对应用户路径已经恢复。正式发布还必须满足：

- 本次相关检查通过，已验证源码与部署源码一致；
- `wrangler.jsonc` 仍指向现有 Worker `zhuwei` 和现有 `DB`，D1 迁移状态清楚；
- Cloudflare 控制面显示新版本已接收预期流量，代表性入口成功或已明确记录传输层限制；
- 只有代表性生产探针成功时才写“外部能力已恢复”；探针失败时写明“安全失败路径已验证，外部能力未恢复”及恢复条件；
- `cloudflare` 分支提交并推送，远端 `main` SHA 不变；
- 回执只列本次实际执行的检查、版本、迁移和未解决限制，不把未执行项写成已验证。
