# 烛帷项目级代理工作流

本文件只指导 agent 如何确定任务范围、读取权威依据、实施、验证和交付。产品规则写在 SPEC，技术决定写在 ADR，实现位置与运行配置由仓库和导航文档提供，专项计划与授权写在对应任务文档；这些内容不在本文件复述。

只在 `cloudflare` 分支工作，保持远端 `main` 不变。开始修改前检查分支与工作区差量，保留用户已有改动。仓库及受保护部署的具体边界按 [README](README.md) 与 [仓库边界 ADR](docs/adr/0013-v3-product-generation-and-repository-boundary.md) 核对。

## 产品需求与规格权威

- [SPEC 0001](docs/specs/0001-llm-kp-responsibility-contract.md) 是主 PRD，也是产品行为的最高文档权威。设计、实现、Prompt 与测试均须满足其行为边界。
- 已批准或已裁定的补充 SPEC 细化主 PRD，不因实现更方便、代码已经偏离或存在另一种合理设计而自动失效。先从 [规格索引](docs/specs/README.md) 定位，再核对原文的状态和取代范围；草案、已被替代或未批准的内容只用于审计。
- 默认不修改已批准或已裁定的 SPEC。只有用户明确要求的产品结果与具体条款实质冲突时，才指出冲突、影响和拟修改条款，提出一个最小确认问题；取得明确确认后，才能修改该 SPEC 及其直接消费者。用户未确认时，不修改规格，也不实现与其冲突的行为。
- SPEC 只冻结其明示的产品行为；未决定的实现选择不能从现有代码反推为已冻结。已接受的 ADR 是当前技术决策依据，可以被后续明确决定取代。
- [CONTEXT.md](CONTEXT.md) 统一领域语言；代码、测试和实现导航提供实现证据，不能反向定义或削弱产品合同。上游仅补足当前用户决定与已裁定 SPEC 未覆盖的既有基线。

## 文档读取路由

- 恢复既有行为：先定位主 PRD 的对应条款或验收场景，再读一个直接相关的补充 SPEC、入口、直接依赖和已有测试；证据不足时沿直接依赖补读，不默认遍历全部规格或 ADR。
- 开发新能力：先定位主 PRD 对目标和边界的约束，再读直接补充 SPEC 与现有端到端入口。新增或改变预期产品合同时，完整阅读主 PRD，再形成能力合同和代表性验收矩阵。
- 修改领域边界或架构：加读 `CONTEXT.md` 与直接相关 ADR，核对公共接口、直接消费者和持久化边界，再按「开发与架构变更」判断。
- 定位实现与运行环境：从 [repo map](docs/agent/repo-map.md) 找入口，以源码、`package.json` 和实际配置核实。导航是有日期的快照，不能代替当前仓库事实。
- 处理专项任务：从 [任务与指导文档索引](docs/agent/README.md) 找到对应指导、计划和用户授权。只继承明确适用于当前任务及对象范围的决定，不把历史任务自动接续为本轮目标。
- 修改需求、修复规格相关 Bug、补验收门、整理规格或维护检查基线：先加载 [spec-workflow](.claude/skills/spec-workflow/SKILL.md)。

## 开发期任务路由

普通任务默认处于开发期，先按用户意图区分范围与停止条件：

- 新增、扩展、补齐或泛化一类行为，走「能力开发闭环」。
- 已有行为偏离主 PRD、已裁定 SPEC、已承诺基线或既有正常路径，走「Bug 修复闭环」。用户只描述症状也可以开始排查，截图和复现步骤是可选线索。
- 同时包含新能力和回归缺陷，分别写明两部分验收边界，不能把整个任务缩成一个最窄症状。
- 纯文档或代理说明调整，核对内容归属、直接引用与流程约束，按文档检查交付，不套用代码开发矩阵。

用户使用“比如”“例如”等表达时，例子默认是能力的代表性验收样例。交付对象是该能力及其声明的变化维度，不能用例子的名称、数值或专用分支替代能力实现；只有用户明确限定或规格定义为封闭枚举时才按单一实例实现。

只有存在两种以上合理的期望行为，且选择会造成实质不同的产品结果时，才提出一个最小澄清问题；否则直接推进。

任务完成、跨层或多文件修改、公共接口变化、最后一次小修和影响面暂不明确，都不自动进入全量验证或发布。阶段切换按「并行与发布路由」执行。

## 能力开发闭环

1. 从用户目标、主 PRD 和直接补充 SPEC 提炼一句能力合同：使用者、对象类别、支持的变化、关键状态转换、权限与失败边界、持久化归属和可见结果。写明用户例子验证哪个变化维度。
2. 沿“真实输入 → 权限/状态 → 编排 → 持久化/外部依赖 → 可见结果”找到现有接缝，用代码搜索枚举修改点的直接调用者、消费者和序列化边界。
3. 动手前建立最小代表性验收矩阵：用户样例、一个触发同属该能力但结构不同的变化维度的样例，以及一个最高风险的拒绝、边界或既有正常路径。矩阵不扩展未要求的相邻功能。
4. 在单一事实源实现能力，让不同样例经过同一通用路径。若目标是可扩展机制，完成机制及其闭包边界，不必穷举所有领域实例。
5. 跑通代表性矩阵与直接消费者。只有声明的能力、变化维度和端到端事实链均闭合后才停止；未要求的相邻能力记入未覆盖范围。

## Bug 修复闭环

1. 阅读相关入口、直接依赖和已有测试，用最窄路径复现症状，保留能定位层级的失败请求、状态、错误或 diff。
2. 沿输入、权限与状态、编排、持久化或外部依赖、可见结果定位第一个违反不变量的位置，用一句话记录根因及证据。
3. 用代码搜索枚举修改点的直接调用者、消费者、持久化写入和公开输出，只检查实际存在的直接影响。
4. 在拥有该不变量的事实源做最小修复，不做无关重构，不用吞错、伪造成功、备用路径、硬编码返回或自动替换外部依赖掩盖故障。
5. 验证原失败路径、一个直接相关的正常路径和一个最高风险的直接连带路径；原失败路径已覆盖其中一项时不重复增加。

直接连带范围限于共用本次根因或被修改不变量的事实源，以及因果链相邻边界：直接调用者或消费者、同一状态转换的前后态、允许/拒绝或首次/重试等对偶路径、数据直接经过的序列化与持久化或公开输出。仅代码位置相邻不构成直接连带。

原症状、正常路径和最高风险直接连带路径通过，直接消费者逐一判定、本级检查通过且没有新的无法解释失败后，停止扩展。定向证据仍不足时，报告缺口和建议追加的一项检查，不自行升级全量验证。仅能支持相关性的证据明确标为推断。

## 开发与架构变更

- 实施前先核对现有依赖、平台原生能力和真实消费者，选择满足验收条件的最简单方案；已有能力足够时不添加新包、预防性抽象或备用路径。
- 跨层能力先完成一条贯穿真实入口至可见结果的端到端路径，再扩展已声明的变化维度。“最小”限制无关范围，不能削弱产品合同或把能力缩成单一示例。
- 按职责和变化原因划分实现，按现役公共接口验证行为；接口被取代时同步直接消费者和测试。展示、适配与测试代码遵守已裁定的职责边界。
- 删除实现前核验发布状态、持久化引用、公共接口、现役数据与恢复消费者。无消费者的开发态旧实现，在替代路径闭合后同次删除相关入口、类型、测试和样式，不为其建设兼容或迁移。涉及现有数据时按适用的兼容合同和明确授权处理，一次性例外不外推到其他数据或后续版本。
- 当前架构阻碍目标时，先区分产品不变量与实现选择，再核对直接 ADR、入口、消费者和持久化边界。替代方案不改变产品结果、持久化兼容、安全姿态、远端资源或外部成本时，可在任务内重构并新增或取代 ADR；触及上述实质变化且尚无适用授权时，说明影响并提出一个最小确认问题。
- 替代路径闭合后同步实现导航、直接消费者和验证门。测试和模块门是决策的实现证据，不能冻结已被取代的内部结构；保留产品不变量和公开行为的验收证据。
- 权威文档和既有基线未规定的交互，先参考成熟同类产品与平台惯例，再选择符合当前合同的模式，不因此扩大任务范围。

## 开发期验证

默认最多选择三类直接证据，同一源码状态下每项运行一次：

- 纯文档、注释或代理说明只检查目标段落、相关链接与约束、`git diff --check` 和最终 diff，不运行代码测试。
- Node/`.mjs` 测试直接运行目标文件或用例，如 `npx tsx --test tests/<target>.test.mjs`；Vitest 测试直接运行目标文件，如 `npx vitest run tests/<target>.test.ts`。先核对 `package.json`，不以会展开全量用例的脚本代替定向测试。
- 只有共享或导出类型、公共签名或 DTO 改变时运行一次 `npm run typecheck`。
- 只有目标文件实际受 Lint 配置覆盖且风险属于静态规则时才检查相关文件；被忽略的文件不能记为已验证。
- 能力开发用同一目标用例组覆盖代表性矩阵，至少证明用户样例与结构不同的样例走同一通用路径，并覆盖最高风险边界。跨层能力至少有一个样例贯穿真实本地接口。
- Bug 修复覆盖原失败、直接相关成功及最高风险连带路径。跨层 Bug 至多增加一条真实本地接口成功路径和一个最高风险失败路径，不重复已有覆盖。
- 修改持久化结构时，先定位 schema 事实源与生成配置，运行对应生成命令并逐行检查新增 migration，再在本地完成一次最小迁移与写入—读取闭环；已生成 migration 只增不改。
- 浏览器 QA 仅在验收对象是视觉或真实交互且确定性测试不足时执行，只检查目标路径或能力矩阵中的代表性路径。
- 真实外部探针仅在结果会决定设计或修复、或用户明确要求确认外部恢复时，在适用授权和预算内执行一次有界默认组合；首个结果仍不能区分边界时才追加一次对照，随后停止。先读取专项任务的批次预算与停止条件，记录脱敏结果。

开发期不运行 `npm test`、全项目 Lint、production build、远端 migration、部署命令或 Git push。构建或测试运行器本身是故障对象时，可运行对应完整命令一次复现原症状。已有行为测试覆盖验收条件后，不增加同义源码正则、重复截图或第二套端到端测试。

## 规格工作流

SPEC 记当前规则，ADR 记决定的原因与取代关系，回执记当次验证证据。**已有行为偏离已裁定 SPEC 时默认是 Bug，不是规格过时。** 产品变更先按「产品需求与规格权威」处理，再依 [spec-workflow](.claude/skills/spec-workflow/SKILL.md) 同步条款、ADR、索引、直接引用和验收门。

维护文档时，逐段判断是流程、产品规则、技术决定、实现导航、任务授权还是历史证据，写入对应事实源。删除或搬走内容前核实承接位置；唯一信息先迁入合适文档，再同步入口与相对链接。文档整理不改变产品行为或扩大已有授权。

规格相关 Bug 在修复代码或测试中标注 `SPEC NNNN §x.y`。规格引用、条款、frontmatter、验收门和基线的检查与更新步骤由技能统一维护；不得为变绿而登记新违规、扩大 allowlist 或更改行为断言。检查基线不构成开发期运行全量测试的授权。

## 交付与记录

交付说明包含结果、直接影响、实际验证及未覆盖范围。普通开发与纯文档调整不因“发生了修改”就新增日志或独立回执；只有适用流程要求，或证据需要独立留存时，才创建对应记录。

文件改动由 git 保存，裁定进 ADR，需留存的验收证据进 [回执目录](docs/agent/receipts/)，当前检查状态由工具生成。相同信息只记录一次。

只有需要保留、且上述载体未承载的排查过程或未覆盖范围，才追加日志，例如事故排查中的测量与被排除假设。记录追加到当月 `docs/refactor-log/YYYY-MM.md`，跨月更新 [日志索引](docs/refactor-log.md)；需要历史证据时按关键词搜索，不通读日志。

migration、部署、push 与并行集成按对应流程保留操作审计，不在多个文档重复记账。

## 并行与发布路由

单一能力纵切或一个 Bug 及其直接影响默认由当前 agent 在当前 worktree 完成。只有至少两个可独立验收、修改路径不重叠且能产生实际并行收益的范围，才完整读取 [并行流程](docs/agent/parallel.md) 并按其隔离要求执行。

只有用户本轮明确要求完整回归、发布、部署、远端 migration、Git push 或里程碑冻结时，才完整读取 [发布流程](docs/agent/releases/release.md) 并切换阶段。“修好”“完成”“检查一下”或普通交付不构成发布授权；各类外部操作分别核对明确授权的对象和范围。

创建远端资源、修改 Secrets、远端 migration、部署和 Git push 须有适用于当前操作的用户明确授权。已授权的专项例外从对应任务文档读取，不重复请示，也不扩展到无关对象、后续版本或其他外部操作。
